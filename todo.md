# Adwitiya FTV — Task Tracker

Status legend: ✅ done · 🟡 in progress · ⬜ pending · 🟥 blocked · ⏸ deferred

> Update this file at every status change. New items go at the bottom of the current phase. Keep one-line per task; details go in commit messages.

---

## Phase 0 — Foundation ✅

- [x] Install Node 22 LTS via winget
- [x] Move legacy HTML files to `/legacy/`
- [x] Scaffold Next.js 15 + TypeScript + Tailwind
- [x] Prisma schema + SQLite dev DB + first migration
- [x] Auth.js v5 credentials + bcrypt + admin seed (ankur/ankur@123)
- [x] Base layout: sidebar, topbar, login page
- [x] Esc-key unsaved-changes hook + DD-MM-YYYY util

## Phase 1 — Core Masters ✅

- [x] Vendor Master: CRUD, search, CSV import/export, unique-code enforcement
- [x] Item Master: CRUD, HSN, vendor dropdown, model enum, image upload, popup viewer, "open in new tab"
- [x] ItemPriceRevision history (latest shown in list)
- [x] Category management with tree hierarchy
- [x] Initial commit `4fb8cdc`

## Phase 1.5 — Multi-vendor users, ops, UI polish 🟡

- [x] Write `CLAUDE.md` (project brief)
- [x] Write `todo.md` (this file)
- [ ] Dev script `scripts/dev.ps1` (stop-if-running + restart)
- [ ] Stop script `scripts/stop.ps1`
- [ ] Deploy script `scripts/deploy.ps1` (migrate + push)
- [ ] Validate env vars at startup (`src/lib/env.ts` Zod schema)
- [ ] Expand `Role`: `ADMIN | VENDOR_ADMIN | VENDOR_USER`
- [ ] Add `VendorInvite` model (token, email, role, expiry)
- [ ] Add `User.isActive`, `User.lastLoginAt`
- [ ] `rbac.ts` helpers: `requireAdmin()`, `requireVendor()`, `getScope()`
- [ ] `audit.ts` wrapper used by every server action
- [ ] Admin pages: `/users` — list, invite (admin or vendor_admin), deactivate
- [ ] Vendor admin: invite vendor_users within their vendor
- [ ] `/invite/[token]` accept-invite page (sets password, activates account)
- [ ] `/vendor` portal shell — landing, scoped sidebar
- [ ] Middleware redirect by role (`ADMIN` → `/dashboard`, vendor roles → `/vendor`)
- [ ] Update vendors page: only `ADMIN` sees full list; vendor users see their own row
- [ ] Update items page: vendor users see only their own SKUs
- [ ] Add `react-hot-toast` for non-blocking notifications; replace `alert()`
- [ ] Add `tailwindcss-animate` for fades, slides
- [ ] Loading skeletons on tables
- [ ] Smooth modal transitions (image popup, dialogs)
- [ ] Search debouncing (250 ms)
- [ ] Optimistic deletes (remove row, rollback on error)
- [ ] Empty states with icons + friendly copy
- [ ] Bcrypt rounds 10 → 12
- [ ] Password policy: min 10 chars, mixed case + digit
- [ ] Security headers in `next.config.ts`: CSP, HSTS, X-Frame-Options, Referrer-Policy
- [ ] Per-IP rate limit on `/login` and `/invite/[token]` (in-memory; upstash in Phase 2)
- [x] Code review pass via subagent against this checklist
- [x] Fix critical: requireAdmin on item + vendor server actions
- [x] Fix high: uploads.ts path traversal + extension trust
- [x] Fix high: invite acceptance atomicity + unified error
- [x] Fix high: login timing leak (constant-time bcrypt + IP rate limit)
- [x] Fix high: setUserActive last-admin race (transaction)
- [x] Fix high: rate-limit memory eviction
- [x] Fix medium: NEXT_PUBLIC_APP_URL via env.ts, role validation
- [x] Schema indexes + onDelete cascades
- [ ] (Phase 2 follow-up) CSP nonce pattern via middleware to drop 'unsafe-inline'
- [ ] (Phase 2 follow-up) Audit log viewer page
- [ ] Commit Phase 1.5

## Phase 2 — Transactions ✅

- [x] Purchase Order: multi-row builder, vendor dropdown, due date, auto PO number, total calc
- [x] PO PDF (react-pdf): org letterhead + vendor block + SKU rows + image thumbnails
- [x] PO email stub (console logger; Resend wiring deferred)
- [x] PO WhatsApp deep link (wa.me)
- [x] PO Pending column (after partial GRN)
- [x] Close PO button + Delete PO button
- [x] GRN: multi-row, type Purchase/RTV, vendor invoice ref
- [x] GRN: PO auto-fetch FIFO by SKU, PO# + date popup under each row
- [x] GRN: batch no auto per GRN, batch exp = GRN date + 120d
- [x] Sales: import (Date, Marketplace, SKU, Qty Sold/Return/RTO, Txn Type)
- [x] Sales: filters + bulk select + admin-only delete
- [x] Sales: export filtered CSV + email filtered view
- [x] Stock Report (Net Inward − Sale + Return + RTO = Balance)
- [x] Warehouse Transfer with SKU code visible + type/location/qty
- [x] Vitest setup; date.ts + rate-limit.ts unit tests (9 passing)
- [x] Code review pass — 1 critical + 3 highs + several mediums, all addressed
- [x] CSV formula injection escape (sanitizeCell)
- [x] SSRF guard in pdf-po (imageUrl allowlist)
- [x] PDF route explicit 401/403 gate
- [x] GRN concurrency: Serializable transaction in Postgres (best-effort in SQLite)
- [x] suggestPoForSku scans candidates for actual pending qty
- [x] bulkDeleteSales / bulkDeleteGRNs surface errors + per-row audit
- [x] Sale.vendorId snapshot (prevents re-attribution if Item moves vendors)
- [x] Commit Phase 2

### Phase 2 follow-ups
- [x] Document `Series` UI (admin edits prefix/padding; refuses to decrease nextNumber)
- [x] Audit log read-only viewer (admin) — entity/action/actor/date filters; off-by-one fix
- [ ] Playwright smoke test (login → create vendor → create item → create PO) — still open
- [x] Real Resend wiring — env-detected; throws if API key set without FROM_EMAIL
- [x] Move uploads from local filesystem to Vercel Blob — env-detected via BLOB_READ_WRITE_TOKEN

## Phase 3 — Payments & Reports ✅

- [x] Payment model + unique (vendorId, month, model) + migration
- [x] /payments page: pivot on Sales by (vendor, month, model)
- [x] Month + Model + Vendor dropdowns + search
- [x] Status PENDING/PARTIAL/PAID + amountPaid + UTR + paidOn + remarks
- [x] Zod cross-validation: PAID needs UTR + amount > 0; PENDING blocks amount > 0
- [x] Bank Payable CSV export (NEFT-format with sanitizeCell)
- [x] Vendor Ledger: vendor filter, Purchase + Payment + RTV rows, running balance
- [x] Ledger summary cards: total debit / credit / balance owed
- [x] Daily Sale/Return report cron `/api/cron/daily-report` (Vercel Cron at 18:30 UTC = 00:00 IST)
- [x] Cron auth: bearer-token; refuses to run in prod without CRON_SECRET
- [x] IST-anchored window for daily report (correct Indian calendar day)
- [x] Code review pass — 1 critical + 3 highs + several mediums, all addressed
- [x] Commit Phase 3

## Onboarding — Public self-serve application ✅

Reverses the earlier "invite-only" decision. Public `/apply` flow lets prospective
vendors apply directly; admin reviews + approves with auto-invite.

- [x] Schema: 16 new optional Vendor fields (businessType, yearsInBusiness, contactName, designation, website, referralSource, productCategoryHint, productCountRange, priceRange, catalogLink, samplesLink, applicationNotes, accountType, branch, gstCertUrl, chequeUrl) + appliedAt/reviewedAt/reviewedById/reviewNotes
- [x] EmailOTP model with consumedAt + Vendor.email @unique
- [x] OTP lib: 6-digit code, bcrypt-hashed, 10-min TTL, attempt counter, 30s resend throttle, lazy GC of expired rows, surfaces real Resend errors
- [x] `/apply` 6-step wizard: email+OTP → business → contact → catalog → bank+docs → review → submit
- [x] `/api/onboarding/{otp,verify-otp,upload,submit}` endpoints (rate-limited per IP + per email, MIME-allowlisted PDF/image uploads, email-must-be-verified gate)
- [x] `/vendors?status=PENDING` filter + per-row "Review" button
- [x] `/vendors/[id]/review` admin review page with full application read-only + approve/reject actions
- [x] Approve = atomic status flip (`updateMany where status=PENDING`) + auto-create VendorInvite + email applicant the invite link
- [x] Reject = atomic status flip with required review notes
- [x] PDF added to upload MIME allowlist
- [x] Login page has "Apply to onboard" link
- [x] Code review pass — 0 critical, 4 high + 3 medium, all addressed
  - Vendor.email @unique + DB-level race protection on submit
  - Atomic approval / rejection (concurrent-admin safe)
  - OTP send-failure now propagates to user when Resend configured
  - Verified OTPs consumed after submit + isEmailVerified excludes consumed
  - Crypto-random vendor code suffix (was Math.random)
  - PII minimised in audit payload (email no longer logged)
  - Generic "received" response on duplicate email (no enumeration)

## Phase 4 — Vendor Portal Completion ✅

- [x] Read-only POs (own only) — list + detail at /portal/purchase-orders
- [x] Read-only GRNs (own only) — list + detail at /portal/grn
- [x] Read-only Sales (own SKUs only) — /portal/sales with filters + CSV export
- [x] Payment status view — /portal/payments (pivot on own sales by month + model)
- [x] Ledger view — /portal/ledger (own GRN + Payment with running balance)
- [x] Server-side enforcement via requireVendor() + vendorId filter on every query
- [x] [id] detail pages 404 on cross-vendor ID
- [x] VendorSidebar: Phase tags removed, Team link adminOnly
- [x] Code review pass — no critical/high/medium findings, 2 LOW fixes applied
- [x] Off-by-one fix on sales `to` filter (mirrored in admin)
- [x] Commit Phase 4

## Phase 5 — GST Compliance ⬜

- [ ] GSTR-1 (outward supplies) export
- [ ] GSTR-3B summary
- [ ] GSTR-2A reconciliation view
- [ ] Code review + commit

## Phase 6 — Advanced Ops ⬜

- [ ] Role Matrix UI (granular permissions)
- [ ] Ticket System (raise, assign, track, SLA)
- [ ] Backup/Restore (DB + Blob manifest)
- [ ] Multi-Warehouse stock segregation
- [ ] Shipping Tracker (logistics + tracking #)
- [ ] Agreement repository
- [ ] KPI Board (Recharts: sales trends, top SKUs, vendor performance)
- [ ] WhatsApp Cloud API upgrade
- [ ] Stock Adjustment, Rejection Out, Other Charges, Order Import
- [ ] Profile Management
- [ ] Code review + commit

## Correction batch — 27-05 (requested) ⬜

### Item Master
- [ ] Bulk import via CSV/Excel — "Import" button, strict file validation, clear per-row error messages
- [ ] Quick Export: Categories (with filter)
- [ ] Quick Export: Vendor Codes (with filter)
- [ ] Quick Export: Models (with filter)
- [ ] Price update flow that appends an ItemPriceRevision (history kept) — already modelled; needs UI; resolved by txn date in PO/GRN/Sale/Payment

### Purchase Order
- [~] Bulk import POs via CSV — DROPPED per user (29-05)
- [x] Price-mismatch popup: inline amber flag + confirm on submit — commit `345732a`
- [x] HSN column on PO (builder + view + PDF, from Item Master) — commit `563fdc1`
- [x] Due Date MANDATORY — block PO generation without it (commit `2247cf4`)
- [x] PDF: org GSTIN already printed; org address expanded (commit `2247cf4`) — set exact PIN later
- [~] PDF: user-editable PO format/template — DROPPED per user (29-05)
- [ ] (cleanup) resolve the stray "Also Remove ??" note with user

### New module — Reject Out / Return to Vendor (left panel, below GRN)
- [x] RTV entry like GRN but REDUCES inventory (QC rejection of purchased goods) — dedicated /rtv module reusing the GRN builder (type locked to RTV) + RTV-only list. Already reduces stock (Stock Report) and posts a Return-to-Vendor debit in the ledger.

### Sales
- [x] "Record Sale" manual-entry form (/sales/new) — rate+tax from Item Master by date — commit `<record-sale>`
- [ ] Fix: Type = Return throws an error — NEEDS LIVE REPRO (manual form handles RETURN fine; confirm import/filter + capture message)
- [x] Collapse/expand left sidebar button (global) — admin + vendor, commit `d144545`

### New module — Other Charges (left panel)
- [x] Post charges that DEDUCT from vendor ledger: Date, Vendor (dropdown), Reason, Taxable, GST, Total — /other-charges (OC- doc series), posts as debit in admin + portal ledger (commit pending)
- [x] CSV export + vendor-wise filter
- [ ] PDF / Email per charge (deferred — low priority)

### Stock Transfer
- [ ] Bulk import button (CSV)
- [ ] Export button
- [ ] Filter button
- [ ] Add Document No. + Date + Transfer Type (SJIT / SOR / Other + create-new)

### New module — Stock Adjustment
- [x] Manual +/- inventory per warehouse to reconcile physical vs system; one-by-one + bulk import — /stock-adjustments (SA- doc series), signed qtyChange, optional warehouse, filters + CSV export/import, flows into Stock Report balance (new Adj column)

### Stock Report (detailed)
- [x] Main Stock Report: vendor + model filters + CSV export (filtered) — also shows Adj column
- [x] Warehouse-wise inventory position with date filter — /warehouse-stock. Per user decision (per-document warehouse picker): GRN + Sale + Record-Sale form + sales CSV import now carry an optional warehouseId. Legacy rows show under an "Unassigned" column.
- [x] Columns: SKU Code · Item Name · Vendor · Model · Warehouse-N… · Total (Total = full balance, matches main Stock Report). Vendor/model/warehouse + date filters + CSV export.

### KPI Board — DROPPED per user (29-05)
- [~] Admin KPI dashboard
- [~] Vendor KPI dashboard

### Masters
- [x] Warehouse Master: create/edit, address, auto-generated Warehouse ID (WH-001…) — /warehouses
- [x] Model Master: full CRUD list (create/edit/delete) + label/remarks/return-policy/active/sort; feeds Vendor + Review dropdowns — /models. Vendor validation now accepts any active Model Master code (isValidModel), not a hardcoded enum (commit `7fad218`)
- [x] **#87 Address normalization** (vendor + warehouse): Vendor now carries structured `city`/`state`/`pincode` alongside the legacy `address` blob; state is a 28-state + 8-UT dropdown from `INDIAN_STATES`; pincode is regex-validated (`[1-9][0-9]{5}`). Warehouse gained the same split, plus an `OWN | THIRD_PARTY` ownership type with a vendor FK (required for THIRD_PARTY, forbidden for OWN — enforced in zod + server action). PO PDF "Vendor" block and PO view "Vendor" card now print the full structured address. Vendor CSV import/export round-trips the new columns. Rollback tag: `pre-vendor-address-split`.
- [x] **Warehouse GSTIN**: per-warehouse `gst` column (nullable; same 15-char regex as vendor GST). Each Indian state where Adwitiya operates needs its own GSTIN, so each state's warehouse gets its own — feeds the ship-from GSTIN on invoices and the IGST vs CGST+SGST split later. Surfaced in the Warehouse Master form + list table. Document/PDF wiring will come with #96 (Company Profile + multi-GSTIN registry).
- [x] **Country (vendor + warehouse)**: ISO 3166-1 dropdown — full ~195-country list with India pinned at the top + the rest alphabetical (`COUNTRIES` const in `lib/constants`). Defaults to "India" on create (`DEFAULT_COUNTRY`). Surfaces in both forms next to State/Pincode, in the vendor review page, and in the vendor CSV export/template/import. Future-proofs overseas vendors (importers/exporters) and warehouses outside India.
- [x] **#96 Company Profile + multi-GSTIN registry** (the SaaS bridge step): two new tables — `Company` (legalName, brandName, PAN/TAN/CIN, full address, contact, currency, FY start, default bank) and `CompanyGSTIN` (one per state, PPOB/APOB, registrationType, isDefault). Single helper at `lib/company.ts` (`getActiveCompany()`, cached + tag-revalidated) is the only API the rest of the codebase uses to read company data — the SaaS migration becomes a single-file change. ORG constant in `lib/constants` is gone; PO PDF + PO view "From" block + email footer now read from the helper. Settings page at `/settings/company-profile` lets admin edit the company + manage multi-GSTIN list with PPOB/default invariants enforced in the server action. Seed creates Adwitiya row + default GSTIN (`09AJLKHJK1CCF`) idempotently. Rollback tag: `pre-company-profile`.
- [x] **Multi-GSTIN wired to Warehouse Master**: the Warehouse create/edit form now cross-references the selected State against the Company Profile GSTIN registry. Three states: no state → silent hint; matching GSTIN → green chip with the registered GSTIN + "Use this" button that fills the field; no matching GSTIN → amber prompt linking to `/settings/company-profile` to register one. PPOB registrations win over APOB when a state has multiple. First concrete consumer of the multi-GSTIN registry — proves the resolver pattern that will later drive GRN/Sale PDFs.
- [x] **Multi-GSTIN Places (the correct GST model)**: refactored `CompanyGSTIN` to be just the registration certificate (gstin/state/regType/active/default). New child table `CompanyGSTINPlace` holds the addresses — one PPOB + N APOBs per GSTIN. `Warehouse.placeId` FK links an OWN warehouse to its declared Place. Settings page redesigned with expandable GSTIN cards, each showing a nested Places sub-table with add/edit/delete + warehouse-link picker. Warehouse form's previous GSTIN chip became a per-state Place picker: "Declare on GST as [Surajpur HQ ★ · 09AJLKHJK1CCF]" with a "Use this Place" button that auto-fills address + city + pincode + GSTIN in one click. Warehouse list table grew a "Declared on GST" column showing the linked Place + drift warning if `warehouse.address ≠ place.address` (compliance: if these diverge, REG-14 amendment is overdue). Server actions enforce: exactly one PPOB per GSTIN (new PPOB demotes the old), THIRD_PARTY warehouses can't link to a Place (vendor's GST), Place's parent GSTIN state must match warehouse state. Migration done in three phases via `prisma db push` + a one-time backfill script (`prisma/backfill-gstin-places.ts`) that copied each existing CompanyGSTIN's old `address`/`placeType` into a fresh Place row. Rollback tag: `pre-gstin-places`.
- [x] **#93 Customer Master**: bill-to party master mirroring Vendor's address + GST + bank shape (so the multi-GSTIN suggestion chip is reusable later) plus receivables-specific fields — `priceTier` (RETAIL/WHOLESALE/DISTRIBUTOR/MARKETPLACE), `creditLimit` (₹), `paymentTermsDays` (0=COD, 30/60/90=Net N), `salesRep`, `status` (ACTIVE/SUSPENDED/INACTIVE — SUSPENDED = credit hold). Full /customers route: list with search + status + tier filters + CSV import/export/template, /customers/new and /customers/[id] forms, server actions with bulk import (email = match key, overwrite confirm guard). Sidebar entry under Masters group with `alt+c` shortcut. Server action leaves a `TODO (#107)` breadcrumb so the future Accounting Engine knows to auto-create a paired LedgerAccount row (type=ASSET, subType=SUNDRY_DEBTOR) at customer creation. Rollback tag: `pre-customer-master`.
- [x] **#94 Tax Master**: two new tables. `TaxComponent` — seeded taxonomy of all 13 Indian GST components (CGST/SGST/UTGST/IGST/CESS forward + the 4 RCM reverse-charge variants + CGST-TDS/SGST-TDS/IGST-TDS for govt buyers + TCS for e-commerce operators). Admins can rename labels / enable-disable / reorder but not invent new codes (Indian law defines them). `HsnRate` — effective-dated HSN/SAC → slab + cess rows. Resolver lib at `src/lib/tax-resolver.ts` exposes `resolveTaxBreakdown({hsn, fromState, toState, base, date})` returning the full component breakdown: intra-state → CGST + SGST (or CGST + UTGST in Union Territories via `isUnionTerritory()`), inter-state → IGST, CESS added on top whenever cessRate > 0, RCM variants when isReverseCharge, non-taxable supply types (ZERO_RATED / NIL_RATED / EXEMPT / NON_GST) collapse to zeros with the marker preserved for correct GSTR-1 row routing. Returns `unresolved: true` when no HsnRate row matches so the caller can warn admin. Admin pages: `/tax/hsn-rates` (search + filter + inline add/edit/delete + CSS-styled RCM/supplyType chips) and `/tax/components` (read-mostly table with inline name/active/sortOrder edit). Both sit under the Masters sidebar group. Build pulls every existing tax math site (Sale, GRN, MarketplaceOrder, OtherCharge) one resolver call away from being able to drop their hand-coded GST computation. Rollback tag: `pre-tax-master`.
- [x] **#114 Orders unification — Phase A** (additive, no data loss): `MarketplaceOrder` is the unified sell-side ingest now. Two FK columns added: `MarketplaceOrder.warehouseId` (so the order knows which warehouse shipped from) and the symmetric 1:1 link `Sale.sourceOrderId` (`@unique`, `onDelete: Cascade`). New `channel` column on Order distinguishes MARKETPLACE / DIRECT / WEBSITE / LEGACY (LEGACY reserved for Phase C backfill). New helper `src/lib/order-sale-sync.ts` exports `upsertSaleForOrder(orderId, tx)` and `deleteSaleForOrder(orderId, tx)` — projects an Order's `type` (SALE / RETURN / RTO) into the right Sale qty column (qtySold / qtyReturn / qtyRTO) using the existing transactionType union, snapshots model + vendorId via `resolvePrice()` at order.date, idempotent. Marketplace order CSV import rewritten to per-row `prisma.$transaction`. Legacy `/sales/new` manual entry path untouched. Rollback tag: `pre-orders-unification`.
- [x] **#115 Orders unification — Phase B** (backfill links): one-shot script `prisma/backfill-order-sale-links.ts`. Scans every MarketplaceOrder with no paired Sale and either links it to a pre-existing Sale that matches on date+SKU+marketplace+qty (in-JS case-insensitive marketplace compare since SQLite has no `mode: insensitive`) or creates a fresh paired Sale via the same projection the live import uses. Multiple candidates → oldest createdAt wins. Idempotent. Result on dev DB: 7 orphan Orders → 0 linked + 7 fresh Sales (existing Sales were demo-data direct entries not matching the imports).
- [x] **#116 Orders unification — Phase C** (route rename + legacy backfill): `/marketplace-orders` → `/orders`. Sidebar entry relabeled "Marketplace Orders" → "Orders". Legacy `prisma/backfill-legacy-orders.ts` script creates a paired Order with `channel=LEGACY` for every Sale row still carrying `sourceOrderId=null` after Phase B — money reconstructed from `qty × unitRate` + a conservative 50/50 CGST/SGST intra-state breakdown of the snapshot taxRate. Old `/marketplace-orders/page.tsx` becomes a one-line `redirect("/orders")` so bookmarks still work. Old `actions.ts` + `MarketplaceOrdersView.tsx` deleted — the new `/orders` folder is the single source. New "Channel" filter dropdown + badge column on the Orders table (MARKETPLACE blue / DIRECT green / WEBSITE violet / LEGACY gray). Result on dev DB: 12 legacy Sales → 12 new LEGACY Orders. Every Sale now has an Order; the data model is fully unified going forward.
- [x] **#117 Order Transfer Price snapshot + Totals card**: new `MarketplaceOrder.transferPrice` column carries the vendor-side price (what we owe vendor per unit), auto-snapshotted from `ItemPriceRevision` at order date — same lookup the Vendor Ledger uses for payouts, so vendor payout math is identical. Importer accepts an optional `Transfer Price (Vendor Rate)` CSV column; missing → auto-resolves. Backfill script `prisma/backfill-order-transfer-price.ts` stamped all 19 existing Orders (zero unresolved). Order table grew three columns: Sale ₹ · Transfer ₹ · Margin ₹ (per-unit, colour-coded green/red, "UNRES" badge when ItemPriceRevision lookup returns nothing). New **Totals card** above the table — recalculates from the **full filtered set** (uncapped, not just the 1000 row table window), shows: Sales (to Customer) · Transfer (to Vendor) · Gross Margin (₹ + %) · GST Collected · Net Qty · Sales/Returns/RTOs counts · B2B/B2C placeholder. Returns and RTOs subtract from Sales/Transfer/GST so margin is true-net-of-reversals. Foot note clarifies "Transfer Price is display-only for now — vendor payouts still resolve from Item Price History at sale time" so admins don't think edits flow to vendor payments. Step 2 (flip Vendor Ledger to read the snapshot, enabling admin overrides to flow into payment) deferred. Rollback tag: `pre-transfer-price-snapshot`.
- [x] **#118 Record Sale on /orders** (one-off entry path): new "+ Record Sale" button next to Import CSV opens a modal for typing a single Order without CSV round-trip. SkuPicker auto-fills Transfer Price + GST Rate the moment SKU + date settle — calls `lookupOrderDefaults(itemId, date)` which checks `HsnRate(item.hsn, ≤ date)` first then falls back to `ItemPriceRevision.taxRate` for the rate, and uses `ItemPriceRevision.transferPrice` for the vendor side. Sparkles badge says "auto-filled from Item Price History"; amber badge says "no price history — enter manually". Live tax math panel shows Taxable + CGST/SGST/UTGST/IGST/Total recomputing as admin types (intra-state when Place of Supply matches Adwitiya's home state of Uttar Pradesh — TODO: source from Company Profile when warehouse picker lands). Margin per unit shown colour-coded green/red. Submit goes through `createOrder` server action which runs the same per-row transaction as the importer: creates the Order, calls `upsertSaleForOrder()` to push to Sale, logs audit, revalidates `/orders` + `/sales`. Esc closes the modal. Defaults to channel=DIRECT + type=SALE + marketplace="Direct" so counter-sale entry is one click + qty + price. The path admin will use most for non-marketplace daily B2B sales.
- [x] **#119 Vendor-safe Sale rows + privacy cleanup on /orders**: critical regression fix from Phase A — `Sale.unitRate` was being set to `Order.salePrice` (customer side) instead of `Order.transferPrice` (vendor side). `Sale.unitRate` is shared with vendors via the portal as the line-level payable; leaking customer pricing through it would expose retail margins. Fix: (1) `order-sale-sync.ts` now sets `unitRate: order.transferPrice` with an explicit comment saying so. (2) Schema docstring on `Sale.unitRate` rewritten to call out the vendor-side meaning + the portal-sharing concern. (3) One-shot script `prisma/fix-sale-unitrate-to-transfer.ts` rewrote 7 affected Sale rows (the 12 LEGACY-channel rows backfilled from the original Sales were already correct). (4) Per-row **Transfer ₹** + **Margin ₹** columns removed from `/orders` table — admin sees only Sale ₹ on screen; aggregate margin stays in the admin-only Totals card; Record Sale form still shows both prices since admin needs that context to verify auto-fill. Audited `/sales` and `/portal/sales` — neither queries `salePrice`, so the moment the regression fix landed, vendor portal was fully vendor-safe. Vendor Ledger queries unchanged (still resolves via ItemPriceRevision at sale date).
- [x] **#120 Open Margin Report to all admins**: dropped the `User.isSuperAdmin` column and the related `requireSuperAdmin()` / `isSuperAdmin()` helpers from `lib/rbac.ts`. `/margin-report` is now plain `requireAdmin()` — any ADMIN-role user sees it. Removed the Shield / ShieldCheck toggle column from `/users` along with the `setUserSuperAdmin` action. Schema column dropped via `prisma db push --accept-data-loss`. Net effect: no more "ask the super-admin to grant you access" screen on Margin Report; visibility tracks role, not a side-channel flag.
- [ ] **#124 Banking Module** (queued — full scope): multiple `BankAccount` rows for own bank accounts. Per-account `BankTransaction` records (Receipt = money in, Payment = money out, charges/interest/transfer). CSV/XLSX statement import per bank with header-row mapping. Match each imported entry against recorded receipts/payments. **Customer payment recording at Order level** — when a receipt comes in, admin reconciles it against one or more outstanding Orders (which invoice cleared). Reconciliation status per row (MATCHED / UNMATCHED / PARTIAL). Statement-vs-book balance dashboard per account per month. Integrates with Accounting Engine (#107) `LedgerAccount` rows: each BankAccount becomes an ASSET ledger; receipts/payments auto-post journal entries. Estimated effort: ~10-12 hours over 2 sprints once #107 foundations are in.
- [x] **#127 / #124 Banking Phase 1** (Bank Accounts + Transactions + JV auto-post): Two new tables — `BankAccount` (name, bankName, accountNo, ifsc, branch, type SAVINGS/CURRENT/OD/CASH, currency, openingBalance + openingAsOf, isActive) and `BankTransaction` (txnNo via BT- series, date, type RECEIPT/PAYMENT/CHARGE/INTEREST/TRANSFER, amount, refNo, narration, optional customerId + vendorId + contraBankAccountId + contraAccountId, reconciled flag for Phase 2). `ChartOfAccount.bankAccountId` 1:1 link mirrors the Customer/Vendor pattern. `src/lib/accounting.ts` extended with `ensureBankCoA(bankAccountId)` (auto-creates sub-ledger `1120-001`/`002`/… under "Bank Accounts" parent) and `postBankTransactionJournal(txnId)` (direction by type — RECEIPT Dr Bank/Cr counter; PAYMENT flipped; CHARGE defaults counter=5280 Bank Charges; INTEREST defaults 4210 Interest Income; TRANSFER Dr destination/Cr source — all keyed by AUTO_BANK_TXN sourceRefId for idempotency). Constants exported: `BANK_PARENT_CODE`, `BANK_CHARGES_CODE`, `INTEREST_INCOME_CODE`, `MISC_INCOME_CODE`, `MISC_EXPENSE_CODE`. Server actions in `src/app/(admin)/banking/actions.ts` — createBankAccount (auto-runs ensureBankCoA, syncs CoA name on rename + isActive on edit), updateBankAccount, deleteBankAccount (refuses if any txns exist), createBankTransaction (resolves contra from picked Vendor/Customer via ensure-helpers OR falls back to type-specific CoA default), deleteBankTransaction (reverses JV first). Zod validators in `src/lib/validators/banking.ts` enforce TRANSFER needs distinct contra bank. Five routes under `/banking`: accounts list with per-account running balance from JournalLine, accounts/new + accounts/[id] (BankAccountForm), transactions list (latest 200 with delete) + transactions/new (TxnForm — type picker drives which counter fields show; smart defaults shown inline). New sidebar group "Banking" with Landmark icon + `alt+b` for transactions. Series BT- seeded. Smoke-tested: created bank → CoA `1120-001` autocreated → RECEIPT BT-TEST01 ₹5,000 → JV-00028 balanced Dr=Cr → cleanup ran clean; trial balance untouched (still 73 lines, ₹445,186.41 Dr=Cr). Rollback tag: `pre-banking-phase-1`. Phase 2 (statement import + reconciliation) + Phase 3 (order-level receipt allocation) queued under #124 above.
- [x] **#128 / #107 GST Phase 1 — GSTR-1 Outward Supplies**: New `src/lib/gst/` namespace with `period.ts` (monthRange / fpCode / fiscalYearMonths / defaultPeriod / periodLabel helpers) and `gstr1.ts` (buildGSTR1 + gstr1ToPortalJson). Schema additions to `MarketplaceOrder`: `invoiceNo` (unique, nullable), `invoiceDate`, `revCharge` (RCM marker). INV- series seeded; `createOrder` + `importMarketplaceOrders` auto-assign INV-##### for non-MARKETPLACE channel + SALE type (marketplace + RETURN/RTO stay null since they roll into Section 7 / Section 9B and inherit their original invoice). `constants.ts` extended with `GST_STATE_CODES` (37-entry name→2-digit-code map) + `getGstStateCode()` + `getStateFromGstCode()`. `buildGSTR1({period, gstin})` aggregates MarketplaceOrder rows into four sections — **4A B2B** (one row per registered customer's invoice with B2B classifier from `isB2BRegType` + customer.gst), **7 B2CS** (everything else, bucketed by POS state code + rate + type OE/E where E flags marketplace), **9B CDNR** (RETURN/RTO with B2B customer — RETURN/RTO without B2B net out of B2CS bucket), **12 HSN Summary** (qty + value + tax aggregated by HSN+rate). Warnings flag orders missing HSN or missing invoice numbers. Place of Supply resolved from customer.state → placeOfSupply text → warehouse.state → seller state. Filing GSTIN auto-picks the primary `CompanyGSTIN.isDefault=true`. `gstr1ToPortalJson()` emits the offline-tool-compatible JSON shape grouped by ctin. New routes under `/gst`: `actions.ts` (generateGSTR1 + exportGSTR1Json server actions), `gstr-1/page.tsx` (Server Component with period+GSTIN search params), `gstr-1/GSTR1View.tsx` (client component with 5 tabs — Summary tiles, B2B drill-down, B2CS, CDNR, HSN Summary — each with per-section CSV export). New sidebar group "GST Returns" with Percent icon + `alt+g` shortcut. Smoke-tested against dev DB: Apr 2026 → 3 B2CS + 3 HSN + ₹42,990 taxable; May 2026 → 8 B2CS + 4 HSN + ₹58,829 taxable + ₹5,409 GST split; zero warnings. Phase 2 (GSTR-3B), Phase 3 (ITC + 2A/2B reconciliation), Phase 4 (Liability dashboard) queued. Sections deferred for Phase 1: 5A B2C Large >₹2.5L (currently merged into 7), 6A Exports, 9C CDN Unregistered, 13 Documents Issued. Rollback tag: `pre-gst-phase-1`.
- [x] **#129 / #124 Banking Phase 2 — CSV Statement Import + Reconciliation**: New `BankStatementLine` table (importBatchId-grouped, UNMATCHED → MATCHED / IGNORED lifecycle; matching flips `BankTransaction.reconciled=true`). `src/lib/reconciliation.ts` exposes `findAutoMatches({bankAccountId, from?, to?, amountTolerance?, dayTolerance?})` (buckets unmatched txns by direction + integer rupee, scores by amount-diff + day-diff − ref-match bonus, ref-exact gets -5), `applyMatches(proposals, by)` (per-pair $transaction, refuses re-application), `getReconciliationSummary(bankId, from, to)` (statement balance vs book balance from CoA + per-status counts). Server actions in banking/actions.ts: `importBankStatement` (papaparse CSV with header alias mapping — Date/Description/Debit+Credit OR Amount+Type/Ref/Balance — covers most Indian bank exports), `deleteImportBatch` (unreconciles matched txns first), `autoMatchStatement`, `matchStatementLine`, `unmatchStatementLine`, `ignoreStatementLine`. New `/banking/reconciliation` route with bank+period picker, CSV upload, Auto-Match button, 4 summary tiles (warns when stmt vs book diff > ₹1), 3 tabs (Unmatched side-by-side panes + Matched + Ignored), manual-match modal sorting candidate txns by amount proximity. Sidebar gets "Reconciliation" entry under Banking (Scale icon). Smoke-tested: 3 receipts + 3 statement lines (dates +1 day off, one ref matching) → auto-match found all 3 (ref-exact scored -4 vs +1 for others), applied 3 with 0 errors, cleanup clean. Rollback tag: `pre-banking-phase-2`.
- [x] **#130 / #124 Banking Phase 3 — Order-level Receipt Allocation**: New `BankReceiptAllocation` table (bankTransactionId + orderId + amount + allocatedBy; cascade-deletes both ways). `src/lib/allocations.ts` exposes `getOutstandingOrders({customerId?, asOf?})` (SALE orders with total − Σ allocations > 0), `getReceiptOutstanding(bankTransactionId)` (allocated vs unallocated split), `allocateReceipt({bankTransactionId, orderId, amount, by})` (per-allocation $transaction validates: receipt is RECEIPT type, order is SALE, amount ≤ both unallocated receipt AND order outstanding), `removeAllocation(id)`, `getCustomerAgeing(customerId, asOf)` (0-30 / 31-60 / 61-90 / 90+ buckets). Server actions wrap helpers with `requireAdmin` + audit logging + path revalidation. New routes: `/banking/transactions/[id]` (Server Component with 3 KPI tiles — Amount · Allocated · Unallocated — Transaction Details card, + AllocationPanel for RECEIPT type) and `AllocationPanel.tsx` (client component with allocation table + per-row remove + "Allocate to Order" picker modal sorted by amount proximity, supports Full button or custom amount per row). Transactions list rows now link to the detail page via txnNo. Smoke-tested: customer + 2 outstanding orders + ₹2,500 receipt → split ₹1,180 (full) + ₹1,320 (partial) across both; over-allocation rejected with "Only ₹0.00 left" error; ageing put outstanding ₹1,040 in 31-60d bucket correctly; remove allocation restored order outstanding to ₹1,180 + ₹1,040; cleanup clean. Phases 2+3 together complete the #124 Banking Module scope laid out earlier. Rollback tag: `pre-banking-phase-3`.
- [x] **#131 / #107 GST Phase 2 — GSTR-3B Auto-Summary Return**: New `src/lib/gst/gstr3b.ts` with `buildGSTR3B({period, gstin?})` that auto-fills the four roll-up rows admin actually needs each month. Computes **3.1(a) Outward taxable** (sum taxableValue + CGST/SGST/IGST from MarketplaceOrder, RETURN/RTO net out), **3.1(c) Nil/exempt** (gstRate=0 rows), **3.2 inter-state to UR** (bucketed by POS code + rate where customer is UNREGISTERED AND IGST > 0), **4(A5) "All other ITC"** (sum of GRNItem.tax across PURCHASE GRNs from REGULAR-status vendors only, split into CGST+SGST when vendor.state == seller.state, IGST otherwise). Sections **3.1(b/d/e)**, **4(A1-A4)**, **4(B)**, **4(D)**, **5**, **5.1**, **6** kept as zero placeholders (admin enters on portal until later phases). **Section 6 Net Cash Payable** computed as outward − net ITC; surfaces excess-ITC carryforward when negative. New route `/gst/gstr-3b` (Server Component fetches GSTINs + builds report) and `GSTR3BView` client component with 3 summary tiles (Output Tax · Net ITC · Net Cash Payable) + 3 section cards (3.1 with placeholder rows dimmed, 3.2 bucketed table, 4 with placeholder rows dimmed and Total + Net rows bolded). Per-section CSV export covering all rows in canonical 3B format. Print-friendly via `window.print()` (hides filter bar + warnings). Warnings surface vendors marked REGULAR but missing GSTIN + period-with-no-ITC alert. Sidebar GST Returns gets second entry. Smoke-tested on dev DB: Apr 2026 → ₹42,990 outward (CGST + SGST ₹1,864 each, intra) + ₹20,057 inter-state IGST ITC from ₹2,64,300 GRNs (vendor in different state) → IGST carry-forward + ₹3,727 cash payable; May 2026 → ₹58,829 outward including 4 inter-state UR buckets at POS 07/24/27/29 (Delhi/Gujarat/Maharashtra/Karnataka) at rates 3/5/12% → ₹5,409 cash payable, no-ITC warning. Phase 3 (ITC + 2A/2B reconciliation) and Phase 4 (Liability dashboard) still queued under #107. Rollback tag: `pre-gst-phase-2`.
- [x] **#132 / #107 GST Phase 3 — ITC + GSTR-2B Reconciliation**: New `GSTR2BLine` table (importBatchId-scoped lines parsed from portal exports) + symmetric `GRN.matchedItc2bLineId` FK (unique, set-null on delete). `src/lib/gst/itc.ts`: **`parseGSTR2BJson(text)`** handles the portal's deeply-nested b2b/inv/itms shape (data envelope optional, MMYYYY → YYYY-MM, supports both itm_det-nested and flat item formats); **`parseGSTR2BCsv(text)`** maps header aliases (GSTIN of Supplier, Invoice Number, Invoice Date, Taxable Value, Central/State Tax, etc. — covers the offline tool's Excel B2B sheet); **`findItcMatches({filingGstin, period})`** scans UNMATCHED 2B lines for non-matched PURCHASE GRNs from REGULAR vendors, requires exact vendor GSTIN + normalized invoice number match, tax diff ≤ ₹1, scores with -1 bonus for total-value exact match; **`applyItcMatches(proposals, by)`** writes line+GRN flips in a single `$transaction` per pair; **`getItcSummary()`** returns total counts, eligible-ITC tax block (sum of MATCHED line CGST/SGST/IGST/CESS), book-ITC matched + all, and **at-risk** delta (book − matched) — the headline number admin watches. Server actions in `gst/itc-actions.ts`: importGSTR2B (kind json|csv, validates filing GSTIN exists + active), deleteItcBatch (unhooks GRN side first), autoMatchItc, matchItcLine, unmatchItcLine, ignoreItcLine. New `/gst/itc-reconciliation` route: period+GSTIN picker, **JSON import** button + **CSV import** button + **Auto-Match** button; 4 KPI tiles (Total 2B · Matched · Missing in Books · Missing in Portal); Eligible ITC card with CGST/SGST/IGST/CESS breakdown + at-risk warning banner; 5 tabs (All / Missing in Books / Missing in Portal · GRNs panel / Matched / Ignored); manual-match modal scores candidate GRNs and visually flags GSTIN-exact + Invoice-exact + Tax-exact + Total-exact rows in green. Sidebar GST Returns gets third entry. Smoke-tested on dev DB: real GRN-00001 (Anokhi Textiles · ANK/INV/441 · ₹1,34,015) + synthetic 2B JSON with one real line + one fake → auto-matched the real one (Δ₹0.00 tax · total exact), left fake as missing-in-books → summary returned eligible ITC ₹8,915, at-risk ₹11,142 (vs total booked ₹20,057 from GSTR-3B 4(A5)). This is the last functional GST piece; Phase 4 (liability dashboard widget) is presentation-only and queued. Rollback tag: `pre-gst-phase-3`.
- [x] **#133 / #106 Multi-Company A1 — Schema (`companyId` foundation + backfill)**: First step of the Tally-style multi-company migration. `prisma/add-company-id.mjs` is an idempotent one-shot that adds `companyId String?` + `company Company? @relation(...)` + `@@index([companyId])` to 21 operational models (Vendor, Category, Item, Warehouse, Customer, PurchaseOrder, GRN, Sale, Payment, WarehouseTransfer, OtherCharge, StockAdjustment, OrPayment, MarketplaceOrder, MarketingCost, MarketplaceRate, Series, ChartOfAccount, JournalEntry, BankAccount, BankTransaction). Company model gets 21 back-relation lists. Stay global: User, VendorInvite, ItemPriceRevision, CompanyGSTIN/Place, TaxComponent, HsnRate, ModelMaster, Unit, line items (POItem/GRNItem/JournalLine/etc.), EmailOTP, AuditLog, BankStatementLine, GSTR2BLine, BankReceiptAllocation (all inherit the company via their parent FK). `prisma/backfill-company-id.mjs` flips every existing operational row to the primary `Company.isPrimary=true` row (Adwitiya) — 183 rows touched across 21 tables; zero null after verification. **`companyId` left nullable** at the schema layer because flipping required produced 248 type errors across ~50 create() call sites; Phase A2 will migrate those one module at a time through `getActiveCompanyId()`. The `flip-company-id-required.mjs` and `unflip-company-id.mjs` scripts stay in the repo as documentation of the planned end-state. New `src/lib/company.ts::getActiveCompanyId()` returns the primary company id today; Phase A2 swaps it to cookie-backed without changing call sites. Rollback tag: `pre-multi-company`. Phase A2 (active-company session + topbar switcher) and Phase B (per-module query scoping + Company B test data + gap pass) queued under #134 / #135.
### Compliance Pillar — MyLegalBook positioning

Owner: brand name `mylegalbook.com` is owned; product positions as
**"Accounting + Compliance for Indian businesses"** — distinct from
Tally / Zoho Books / QuickBooks (all books-first). The compliance
features below turn the "legal" word into a feature differentiator
rather than a naming liability.

- [ ] **#143 MyLegalBook positioning — SEO meta + tagline + landing copy** (~2-3h): Update `<title>` / `<meta description>` / Open Graph tags on every admin page + future marketing site to spell out "accounting software, GST returns, TDS, compliance, India" so search engines + LinkedIn previews route correctly despite the "legal" word. Landing copy leads with the four compliance pillars (Books / Tax Filings / Compliance / Contracts) instead of generic accounting features. Candidate tagline: **"Accounting that keeps you legal."** Alternatives: *"Your books. Your filings. Your compliance. One software."* Pairs with #112 public marketing website.
- [ ] **#138 Companies Act Schedule III — BS/P&L formatter for ROC filing** (~4-5h): Auto-format Balance Sheet + Profit & Loss into the Schedule III (Companies Act 2013) shape with the prescribed line-item hierarchy (Equity & Liabilities → Shareholders' Funds → Share capital + Reserves & surplus → Non-current liabilities → … etc.) and Division II adjustments. Export as XBRL-friendly XML for MCA portal upload. Builds on the existing `getTrialBalance()` + `getBalanceSheet()` + `getProfitAndLoss()` from #125 — adds a Schedule-III line mapper that rolls our 50-account CoA into the 6-7 statutory headings.
- [ ] **#139 MSME registration tracker (Vendor + Customer)** (~3h): Add `msmeNumber`, `msmeCategory` (MICRO/SMALL/MEDIUM), `msmeValidTill` to Vendor + Customer. Auto-flag invoices to MSME-registered vendors aged > 45 days — **Section 43B(h) Income Tax disallows the expense if not paid within 45 days from invoice date** for Micro/Small MSMEs (since FY24, big enforcement priority). MSME payment ageing report + dashboard tile. Required by MSME Development Act 2006 + IT Act compliance. Critical for CAs; nobody else in the SaaS space tracks this well.
- [ ] **#140 Contract / Agreement vault with expiry reminders** (~5-6h): Upload + tag vendor agreements, NDAs, lease deeds, MSME letters, FSSAI licenses, GST registration certs, import/export licenses, partnership deeds. Tag each with `expiryDate` + auto-email reminders 30/15/7 days before. Simple template library (vendor agreement, NDA, MSME confirmation letter, payment authorization). Link to Vendor/Customer records. Audit-trail (who uploaded/replaced what). Uses Vercel Blob (already wired in #29). Differentiator: Tally + Zoho Books have nothing in this space.
- [ ] **#141 Compliance Calendar — GSTR/TDS/ROC/labour due-date dashboard** (~4h): One view of every regulatory deadline relevant to the company: **GSTR-1** (11th of next month), **GSTR-3B** (20th), **GSTR-9** (Dec 31), **TDS quarterly returns** (26Q/24Q/27Q — Jul 31/Oct 31/Jan 31/May 31), **Form 16** (Jun 15), **ITR** (Jul 31/Oct 31), **PT** (state-wise), **PF/ESIC** (15th), **AOC-4 + MGT-7** (60 days post-AGM), **DIR-3 KYC** (Sep 30), **Tax Audit u/s 44AB** (Sep 30/Nov 30). Dashboard widget + email reminders 7d/3d/1d before. Per-company GSTIN-scoped. Marks complete when admin uploads a filing acknowledgement. Combines naturally with #100 Email Configuration + #108 Customer Payment Reminders' email infra.
- [ ] **#147 Income Tax Audit — Form 3CA/3CB/3CD u/s Section 44AB** (~10-12h): Auto-populate **Form 3CD** (statement of particulars — 44 clauses) from books. **Form 3CA** when statutory audit is done elsewhere; **Form 3CB** when no other audit. The 44 clauses cover: deemed business profits (44AD/44ADA/44AE), depreciation method consistency, payments to related parties (Sec 40A(2)(b)), TDS compliance (deducted vs deposited vs returned), MSME 45-day flag (Sec 43B(h) — feeds from #139), GST registration + filings status, cash receipts > ₹2L (Sec 269ST), donations Sec 80G, etc. Required when turnover > ₹1cr business / ₹50L profession. Due **Sep 30**. JSON export for IT portal upload. **Huge CA-workload reducer** — Form 3CD alone takes auditors 5-10 hours per client when done manually; auto-population from books gets them to 1 hour of review. Pairs with #144 ITR Filing + #139 MSME tracker + #146 GST Audit (all four together = "compliance season in one click").
- [ ] **#146 GST Audit — GSTR-9 (annual) + GSTR-9C (reconciliation, turnover >₹5cr)** (~6-8h): **GSTR-9** annual return auto-rolled from 12 monthly GSTR-1 + GSTR-3B. Tables 4 (outward), 5 (exempt/nil/non-GST), 6/7 (ITC + reversals), 8 (ITC as per 2A vs claimed), 9 (tax paid), 10-14 (adjustments). **GSTR-9C** reconciliation statement for turnover >₹5cr — books-to-returns gap analysis with auditor's certificate template + JSON export. **Books vs Returns variance report** — THE thing CAs need at year-end and currently piece together by hand. Reuses #128 GSTR-1 + #131 GSTR-3B data layer + #132 2B reconciliation. Due **Dec 31**.
- [ ] **#144 Income Tax Calculation + ITR Filing (ITR-3/5/6/7)** (~12-15h): Compute taxable income from P&L with Income Tax Act adjustments — **Sec 32 depreciation at IT rates (different from Companies Act!)**, Sec 36/37 allowed expenses, Sec 40A disallowances, Sec 43B due-date payments. Carry-forward losses + brought-forward depreciation. Advance tax quarterly (15-Jun / 15-Sep / 15-Dec / 15-Mar). **ITR-3** (proprietorship with business income), **ITR-5** (partnership / LLP), **ITR-6** (company), **ITR-7** (trust) JSON export for IT portal upload. Mirrors what Cleartax / Tax2Win / Quicko do but **bakes into the books** so admin doesn't re-key — that's the differentiator. Due Jul 31 (non-audit) or Oct 31 (audit).
- [ ] **#145 Recovery Legal Notice to Customer (Demand / Sec 138 / IBC Sec 9 / MSME)** (~5-6h): Auto-generate legal notices for overdue receivables. Template library: **(a)** Demand notice u/s 73 Indian Contract Act (general), **(b)** Sec 138 NI Act cheque bounce notice (15-day cure period — critical timing), **(c)** IBC Sec 9 operational creditor demand (₹1cr threshold, NCLT route), **(d)** MSME 45-day recovery notice with interest u/s 16 MSMED Act. Auto-fills customer name + invoice list + total due + days overdue from Customer Ledger + receipt allocation data (#130). PDF + email + WhatsApp dispatch. Track notice → response → escalation status. Integrates with #108 Payment Reminders (which is the soft-touch precursor; this is the hard-touch escalation when reminders don't work).
- [ ] **#148 Response to Income Tax Notices (intimations, scrutiny, demand)** (~8-10h): Capture IT notices (manual upload first; auto-fetch from portal once #149 ships). Classify by section: **143(1)** intimation, **139(9)** defective return, **142(1)** info request, **148** reassessment, **156** demand, **245** set-off against refund, **154** rectification, **271** penalty, **148A** show-cause. Auto-draft response with assessee profile + relevant computation from books. Track due date (usually 15-30 days from issue). Attach supporting docs. Submit via portal API (where available for CAs) or guide manual submission. Status tracking: received → drafted → submitted → closed / escalated. Notice library for compliance audit trail. **Massive differentiator** — no Indian accounting software does this end-to-end.
- [ ] **#149 Direct portal login (IT + GST + MCA) — credential vault + auto-login** (~6-8h): Secure credential vault (**AES-256 encrypted at rest, audit-logged on every access**) for IT / GST / MCA portal credentials per CompanyGSTIN. One-click launch to portal in new tab with auto-fill bookmarklet or Chrome extension. Where official APIs exist (GSTN GSP/ASP, IT e-Filing API for chartered accountants), use direct OAuth / SSO instead of credential auto-fill. CA / consultant role can hold credentials for multiple client companies (enabled by #134 multi-company). **2FA OTP relay** — forward portal OTP from admin's registered mobile to app for input. Permission-gated: admin-only by default, vendor users never see credentials. Foundation for #148 + #150 + #144 + #146 + #147 — without this, every filing/payment task makes admin manually log in to a different portal.
- [ ] **#150 Direct Tax Payment (Income Tax + GST) — challan generation + NetBanking** (~6-8h): Generate tax challans directly from books — **Challan 280** (Income Tax / Advance Tax / Self-Assessment), **Challan 281** (TDS), **Challan 282** (STT). **GST Payment via PMT-06** challan + auto-top-up Electronic Cash Ledger (CGST/SGST/IGST/CESS heads). Pre-fills CIN, assessment year, head of payment, tax/cess/surcharge breakup from the computed liability. NPCI / NetBanking integration where available; otherwise generate Challan PDF + UPI/IMPS deep link for one-tap payment. Auto-record payment as a JV: **Dr "Tax Paid" or "GST Cash Ledger" · Cr Bank**. Pull CIN back into books on confirmation. Reconciles back to #146 GST Audit + #144 ITR Filing.
- [ ] **#142 e-Invoice / IRP integration (B2B + Export, mandatory FY26+)** (~6-8h): Generate IRN (Invoice Reference Number) via IRP API (gov-mandated providers like Adaequare / NIC) for every B2B + Export invoice. Currently mandatory above ₹5cr turnover, rolling to all by FY26. QR code on invoice PDF. Auto-cancel within 24h window if invoice voided. Cache IRN against `MarketplaceOrder` row. Was deferred from #128 GSTR-1 Phase 1; formally tracked now. Strong differentiator on the "compliance pillar" of MyLegalBook positioning. NIC SP-API has a sandbox.

### Adoption / Switching-Cost Reducer

- [ ] **#161 Tally-canonical keyboard shortcuts — D/A/L/C/Esc/Enter + F-keys + underlined menus** (~18-24h phaseable): **The single biggest "switching from Tally" pain reducer.** Indian CAs and accountants are deeply trained on Tally keystrokes — they navigate by keyboard, not mouse. If MyLegalBook honors the same muscle memory, switching cost drops from "months of retraining" to "days of trying it." Without this, even better features lose to Tally muscle memory. **Canonical universal keys**: **A** = Accept/Save (most-pressed key in Tally), **D** = Display, **L** = List/Ledger view, **Alt+C** = Create new in current context, **Esc** = back/cancel (Tally's universal back), **Enter** = confirm/next field, **Q** = quit, **Ctrl+A** = accept all, **Ctrl+M** = main menu, **F11** = features/settings, **F12** = configure. **F-key voucher shortcuts**: F4 Contra · F5 Payment · F6 Receipt · F7 Journal · F8 Sales · F9 Purchase · F10 Reversing Journal — mirrors Tally Voucher Entry screen exactly. **Underlined-letter menu navigation**: every menu option and button shows one underlined letter (the "hot key"); pressing that letter activates it without mouse. e.g. **C**reate / **D**isplay / **A**lter / **P**rint. **Auto-focus** first input on form load. **Display vs Edit/Alter mode** switching, just like Tally. Visual mode indicator in topbar. **F1** opens contextual help. **Cheatsheet overlay** (existing #88) reorganized to show Tally-equivalents alongside our keys. **Settings toggle**: classic-Tally mode vs modern mode. Pairs with existing #89 (generic keyboard shortcuts in progress) — #89 was generic; #161 is Tally-canonical. **Phase 1** ~8-10h (universal A/D/L/C/Esc/Enter + underlined menus + auto-focus). **Phase 2** ~6-8h (F-key voucher bindings + mode switching). **Phase 3** ~4-6h (cheatsheet + onboarding tutorial overlay). **Should ship in the first wedge release** — dramatic reduction in Tally switching cost makes every other feature 3x more sellable.

### Workflow & Productivity Layer

These three are foundational — once they exist, half the compliance
pillar collapses into "instantiate a template, walk the checklist,
attach the docs, the system creates the tasks." High-leverage to
ship before the heavier filing modules (#144, #146, #147).

- [ ] **#152 Task Management System — assignments, due dates, projects, reports** (~8-10h): General-purpose task engine. **Task** model (title, description, assignee, owner, priority HIGH/MED/LOW, status TODO/IN_PROGRESS/BLOCKED/DONE/CANCELLED, dueDate, completedAt, parentId for subtasks, projectId for grouping). Comments + activity log per task. File attachments. Email + in-app reminders 1d/3d/7d before due. **Kanban + list views**. Reports: overdue by assignee, completion rate, velocity. Integrates as the **execution layer** for #141 Compliance Calendar (each filing deadline auto-creates a Task), #145 Recovery Notices (each notice → follow-up Task), #148 IT Notice responses (response drafting Task), #151 CRM (lead activities). **One unified inbox** for "what does the team need to do today."
- [ ] **#153 Checklist + Template Builder — reusable SOPs with conditional logic** (~6-8h): **Template** model with items[] each carrying title + description + ownerRole + dueDateOffset (e.g. "3d after template applied") + optional file upload + optional conditional logic ("show only if turnover > 5cr"). **Instance** model: which template, when applied, current item status, assigned users. Each item becomes a Task (#152) — Templates are the blueprints, Tasks are the runtime. **Pre-built templates ship out-of-box**: Monthly Closing, GST Filing (auto-applied each period from #141), Audit Prep, New Customer Onboarding, Vendor Onboarding, Year-end Procedures, IT Notice Response Playbook. **CAs love this** — they have firm-specific SOPs they currently run via WhatsApp + Excel. Foundational layer for #141 Compliance Calendar, #146 GST Audit, #147 IT Audit.
- [ ] **#160 Daily Owner Reminder — accountant accountability + business lag signals via WhatsApp/email** (~12-16h phaseable): **Owner's morning briefing**, pushed daily at configurable time (default 9 AM) regardless of whether owner logs in. Distinct from #155 (login-modal for filing deadlines) — this is **proactive WhatsApp + email** that catches problems early. **Two-section format**: **(A) Accountant Accountability** — *"Books not posted for 3 days · Bank not reconciled in 7 days · GSTR-3B filed by accountant? PENDING · Vendor payments overdue: 4 · Customer invoices not raised since Monday."* Each line auto-checked against actual data + #152 task assignments. **(B) Business Lag Signals** — *"Today's sales pace 23% behind same day last week · GP margin slipped 2.1% MoM · ABB trending below ₹X floor · Top customer not ordered in 14 days · AR days extending."* Each metric compares against trailing baseline (7d/30d/YoY) + owner-set thresholds. Delivery: **WhatsApp Business API** (preferred — Indian SMB owners live there), **email** (#100 SMTP), **in-app dashboard tile**, **mobile push** when PWA installed. Owner mutes specific signals + adjusts thresholds. Snooze for weekends by default. Aggregates data from across the system — #125-126 accounting (books-posted check), #124-127 banking (reconciliation status), #128/#131 GST (filing status), #104 TDS (deposit status), #152 task system (overdue assignments), #157 ABB (balance trend), #130 receipt allocation (AR ageing), MarketplaceOrder + Sale (revenue pace). **Turns the software from passive book of record into active fiduciary watchdog.** The owner who has a part-time accountant (very common in Indian SMB) gets visibility they don't have today. **Builds trust + retention** — when the owner sees their books are being maintained on time, churn drops. Phase 1 ~6-8h (signal computation + email + dashboard tile). Phase 2 ~6-8h (WhatsApp Business API + threshold customization). Phase 3: AI-generated narrative on top of the signals.
- [ ] **#155 Statutory Deadline Popup — proactive login-time + push notifications** (~4-5h): Active intervention layer on top of #141 Compliance Calendar. **Login-time modal** when any deadline is within configurable window (default 7d/3d/1d) — *"GSTR-1 due in 3 days for Adwitiya. Open filing module?"* Browser **Web Push** notifications when user is offline. WhatsApp + email reminders via existing email infra. **Configurable thresholds per user role** (CA: 7d only · Admin: 7d/3d/1d · Owner: 1d only — so each role gets right signal-to-noise). Snooze (don't show same alert for X hours). Click-through opens the relevant filing module directly. **"What's Due Today"** tile pinned to top of dashboard. Critical because #141 Compliance Calendar is passive and easy to miss; this layer makes deadlines **unmissable**. Tiny task but huge UX impact — should ship right after #141 + #155.
- [ ] **#154 Document Management System (DMS) — folders, OCR, versions, sharing** (~10-12h): Full DMS, broader than #140 Contract Vault (which becomes a use case on top). **Document** model with hierarchical folders + flat tags. Version control (replace keeps history, restore prior). **OCR for scanned PDFs** so search hits text inside images. Full-text search across all docs. Per-user + per-role access controls. Audit trail (who viewed / downloaded / edited / deleted). Bulk upload + drag-drop. **Secure external sharing links** with expiry + password + view-count tracking. **Attach any document to any record** (Customer/Vendor/Order/Invoice/Payment/GRN/Lead/Task) via polymorphic FK. Mime allowlist + AV scan. Storage in Vercel Blob (already wired in #29 P2 follow-up). **Foundation** that #140 Contract Vault, #148 IT Notice attachments, #146/#147 audit working-papers, #152 Task attachments all build on top of.

### Payroll & HR

- [ ] **#156 Payroll Management — full module** (~60-80h, phaseable): **Employee** master (name, designation, joining date, PAN, Aadhaar, bank, PF/ESIC/UAN numbers, salary structure). **Salary structure** components: Basic, HRA, special allowance, transport, medical, food coupons, other allowances. Monthly **payroll run**: gross → deductions (PF 12%+12%, ESIC 0.75%+3.25% under ₹21K cap, PT state-wise slabs, TDS via #104, loans/advances) → net payable. **Payslip** PDF generation. **Form 16** annual TDS certificate (ties to #104). **Form 24Q** quarterly salary TDS return. **PF + ESIC challan** + ECR file for EPFO + ESIC portals. **Bank salary transfer file** (NACH / Excel for SBI / HDFC / ICICI bulk upload). **Attendance** integration (manual + biometric REST API). **Leave management** (CL/SL/EL/comp-off, carry-forward, encashment). Reimbursements, bonus, gratuity, leave encashment. **Employee Self-Service portal** (view payslip, apply leave, submit IT declarations Form 12BB, upload proofs). Annual: IT declarations + proof submission + Form 16 distribution. **Huge differentiator** vs Tally (no payroll at all) and vs Greytip / Keka / Razorpay Payroll (separate products that users hate context-switching to). **Phaseable**: Phase 1 master + basic run (~20h), Phase 2 returns + portals (~20h), Phase 3 ESS + attendance (~20-40h). Builds on #149 portal credential vault (EPFO/ESIC login) + #150 direct tax payment (PF/ESIC challan) + #152 task system (payroll-cycle checklist) + #100 SMTP (payslip emails).

### Financial Intelligence

- [ ] **#159 Monthly CXO Report Creator — auto-generated investor updates + infographic portal** (~25-35h phaseable): **Recurring monthly investor / board update**, distinct from #158 (one-off fundraising snapshot). Three templates: **Monthly Investor Update**, **Quarterly Board Report**, **Annual Letter to Shareholders**. Each renders **infographic-heavy** sections — Revenue + MoM growth chart, Customer count + churn, Gross margin trend, **EBITDA bridge** (prior period → current with line-item movers), Cash position + runway forecast, AR/AP ageing, Top-5 customers / vendors, Hiring + headcount, **Key wins** (manual notes — owner's narrative), **Asks** (manual notes — *"looking for 2 referrals to FMCG distributors"*). Compare current month vs **previous month, same month last year, plan/budget**. **Investor Portal**: each investor gets a login + sees only the slices they're entitled to (a Series A investor sees revenue + runway; an angel may see less — cap-table-aware section visibility). **Engagement tracking** — who opened the report, which sections they lingered on, time on page (huge fundraising signal for when you raise next). One-click **email distribution** to cap table via #100 SMTP. PDF + interactive web view + Notion-style embed link. **Differentiator** — most SMBs don't send investor updates because they're painful; Carta + Visible.vc do this for tech startups but charge separately ($50-200/month). MyLegalBook bakes it into the ERP. **Pairs with #158**: both use the same data (P&L, BS, cash flow, ABB), but **#158 is the snapshot for new funding conversations · #159 is the ongoing rhythm that keeps existing investors warm and helps them sleep at night**. Phase 1 ~15-20h (templates + auto-generation + investor portal). Phase 2 ~10-15h (engagement analytics + AI narrative draft + cap-table-aware visibility).
- [ ] **#158 Investor Module — auto-generated snapshot, IRR, 5-year valuation, fundability score** (~35-50h phaseable): **At-a-glance business-health dashboard for investors / bankers**, generated entirely from books data we already have. **Snapshot section**: Revenue (current + last 3 years + CAGR), Gross Margin / EBITDA / Net Margin trends, Burn / Runway (for loss-making years), Working Capital + **DSO / DPO / DIO** efficiency ratios, **Customer concentration** (top-5 % of revenue), Vendor concentration. **Fundability Score** (0-100) composite of growth + profitability + capital efficiency + customer-concentration risk + cash position → outputs **FUND / FUND WITH CONDITIONS / PASS** with transparent line-item reasoning. **IRR** computed from historical owner equity contributions vs distributions (data from JournalEntry rows tagged as capital / dividend). **5-year valuation projection** with **bull / base / bear scenarios** using three methods in parallel: **DCF** (projected cash flows × discount rate), **EV / EBITDA multiples** (sector benchmark × current EBITDA), **EV / Revenue multiples** (for growth-stage where EBITDA is negative). Sensitivity analysis on growth rate, margin expansion, working-capital efficiency. **One-page PDF export** ("Series A teaser") with company logo + key KPIs + valuation range. **Shareable link** with optional NDA-gate + view tracking + expiry + watermark. **Data sources all already exist** in the system: #125-126 accounting engine, #124-127 banking, #128/#131 GST turnover, #157 ABB advisor, #130 receipt allocation for receivables ageing. **Differentiator** — only Indian SMB ERP that produces an investor-ready snapshot in one click; most owners pay CAs **₹50K-2L** to prepare this manually for each funding conversation. **Phaseable**: Phase 1 snapshot + score + linear projection (~15-20h), Phase 2 DCF + multiples + scenarios (~10-15h), Phase 3 shareable link + DRM + view tracking (~10-15h).
- [ ] **#157 Average Bank Balance Advisor — real-time loan eligibility + ABB target coaching** (~6-8h Phase 1): Computes **Average Bank Balance (ABB)** + **Average Banking Turnover (ABT)** over 30/90/180/365-day rolling windows from the BankTransaction + BankStatementLine data we already have (#124-127). Maps to **eligibility tables per lender category** — PSB / Private Bank / NBFC / Fintech — using published rules of thumb: OD typically **4-6x ABB**, CC ~**25% of annual sales turnover**, NBFC fintech scores blend ABB + GST turnover (from #128/#131) + ITR data (from #144). Live dashboard card: *"You qualify for ₹X OD at PSBs · ₹Y CC at private banks · ₹Z working capital at NBFCs."* **Advisor coaching is the killer feature** — instead of just showing what you qualify for today, it tells you *what to do to qualify for more*: *"To unlock ₹50L working capital, maintain ₹8.3L more ABB for the next 6 months — don't dip below ₹12L on these dates."* ABB trajectory chart vs target with **alerts when the actual balance dips below the planned floor** (so you can move money before it kills your average). Per-lender card with their specific formula transparently shown. **Phase 2 (~20-30h)**: direct loan-application APIs (Lendingkart / NeoGrowth / Indifi / Razorpay Capital all expose them). **Phase 3**: invoice discounting + BNPL marketplace integration. **Real differentiator** — no Indian ERP shows this. SMBs typically discover lender math only when applying, too late to course-correct. This is the kind of feature that gets posted on LinkedIn and drives organic signups.

### CRM Module

- [ ] **#151 CRM — lead → opportunity → customer (with ERP handoff)** (~10-12h): Lightweight CRM tied to the Customer Master. New **Lead** model (separate from Customer — leads aren't customers yet). Pipeline stages: **NEW → QUALIFIED → NEGOTIATION → WON → LOST**. Activity log per lead (calls / meetings / emails / WhatsApp). Tasks + reminders. Email integration via #100 SMTP. **Quote / proposal builder** → on WON, lead auto-converts to Customer + first Order draft is pre-populated. Sales rep assignment (uses existing User model with VENDOR_USER → SALES_REP role addition). Reports: pipeline value, conversion rate by stage, sales rep leaderboard, deal velocity, lost-reason analysis. Optional **WhatsApp Business API** hookup (Phase 2). Distinct from full Zoho CRM scope — focused on the **lead → order handoff so the data lives in one place**. Pairs with #108 Customer Payment Reminders (post-sale) and #145 Recovery Notices (escalation).

### Multi-Company finishing

> **⚠️ DEFERRED — multi-company is shelved (single-company for now).** The Pattern-1
> multi-company layer (shared DB + a `companyId` discriminator column, Tally/Zoho-Books
> style) built in **#133–#136** is **neutralized**, not reverted: `MULTI_COMPANY_ENABLED = false`
> in `src/lib/company.ts` pins every query to the one primary company (Adwitiya), hides the
> topbar switcher + `/companies` CRUD, and makes `createCompany()` refuse. The `companyId`
> columns + `companyWhere()` scoping stay in place but **inert** — do not extend them.
>
> When multi-company returns (**#106**, Phase 7) it will be rebuilt with **schema-level data
> isolation — a Postgres schema / database per tenant, NOT a `companyId` column**. The current
> `companyId` plumbing is removed cleanly during that rebuild. **#137 (Group Company /
> Consolidation) below is therefore also deferred** — it builds on the shelved layer and will
> be re-scoped onto the schema-isolation model. See CLAUDE.md §4 (Single-company scope) + §8
> (Phase 7).

- [ ] **#137 Group Company / Consolidation — Tally-style multi-entity reporting** (queued, ~5-9h): Tally Prime's `Alt+F3 → Create Group Company` model — admin picks 2+ member companies, system rolls up their books into consolidated reports. **Zoho Books does NOT have this natively** (their consolidation requires Zoho Analytics, a separate paid product); Tally has had it for ~20 years. Schema additions: `CompanyGroup` (id, name, description, isActive, fyStartMonth, baseCurrency, createdAt) + `CompanyGroupMember` (groupId, companyId, includeOpeningBalance, eliminationsEnabled, since/until nullable, joinedAt — composite unique on groupId+companyId). Library: `getActiveContext()` returns `{kind: "COMPANY", id}` or `{kind: "GROUP", id, memberIds: [...]}`; cookie stores both type + id. `getEffectiveCompanyIds()` returns `[id]` for company mode, `[...members]` for group mode. Reports (Trial Balance / P&L / Balance Sheet / Vendor Ledger) take the effective list and SUM across members; **CoA codes consolidate by logical code** — strip the per-company suffix so Adwitiya's "1130" and TestCo's "1130-abc123" both roll into the consolidated "Sundry Debtors" line. **Group mode is read-only** — admin can only post into a member company, never into the Group itself (same as Tally). UI: `/companies/groups` list + `/companies/groups/new` with member multi-select; topbar switcher gets a "Groups" section above individual companies with distinct styling; amber strip at top of every page when Group is active showing "GROUP MODE · read-only · consolidating N companies"; Balance Sheet / P&L / Trial Balance pages render the consolidated view with a "Members: A, B, C" footnote. **Phase 2 — inter-company eliminations** (~3h additional): each MarketplaceOrder / GRN / Payment can be flagged `isInterCompany` with `counterCompanyId`; consolidated reports net those reciprocal transactions out so a sale from Company A to Company B doesn't double-count. Schema field for the flag goes in Phase 1 so it's ready. Builds on top of #134 multi-company foundation; will benefit from #135 list-page scoping landing first. Effort estimate: **~5-6 hours core** (no eliminations), **~8-9 hours with eliminations**.

- [x] **#136 Licensing — HMAC keys + plan catalog + feature gates + activation**: Self-contained, no-phone-home license keys. Format: `FTV-<base64url(payload)>.<hex(hmac)>`. Payload encodes v/plan/seats/maxUsers/features/issuedTo/issuedAt/expiresAt/nonce; HMAC computed with `LICENSE_SIGNING_KEY` env var (with stable dev fallback). New `License` model (key unique · plan · seats · maxUsers · features JSON · issuedTo · status ISSUED/ACTIVE/EXPIRED/REVOKED/REPLACED · issuedAt/expiresAt/activatedAt). Plan catalog in `src/lib/license-features.ts`: TRIAL (1 seat · 3 users · everything except multi-company + API · 30 days free) · BASIC (1 seat · 5 users · core only · ₹999/mo indicative) · PRO (5 seats · 25 users · multi-company + banking + GST + allocation + recon · ₹2999/mo) · ENTERPRISE (unlimited · everything · contact sales). `src/lib/licensing.ts`: `verifyLicense(key)` timing-safe HMAC check + version + expiry; `mintLicense(payload)` for server-side issuance; `getActiveLicense()` (returns status ACTIVE / EXPIRING_SOON within 7 days / EXPIRED); `hasFeature(feature)` (fails closed when license missing or expired); `seatsAvailable()` (used/cap/remaining/unlimited); `activateLicense(key)` (verifies, demotes prior ACTIVE → REPLACED, persists new); `ensureTrialIfUnlicensed()` (auto-mints + activates 30-day TRIAL on first run); `reapExpired()` (idempotent ACTIVE→EXPIRED for past-expiry rows). Server action `activateLicenseAction` validates input and revalidates the whole layout cache. New `/settings/license` page: Active License card with KPIs (Plan / Issued To / Days Remaining / Expires / Seats used+cap / Max Users / Activated / Features count) + status badges; activation form (paste key + validate); full plan comparison table (price + seats + max users + each feature checkmark per tier); license history (last 20 rows, ACTIVE highlighted). New `<LicenseBanner />` Server Component in `(admin)/layout.tsx` runs `reapExpired()` + `ensureTrialIfUnlicensed()` on every request; renders an amber banner when EXPIRING_SOON or red banner when EXPIRED, with "Renew / Upgrade →" deep-link to the license page. **Feature gate on `createCompany`**: blocks 2nd+ company without MULTI_COMPANY feature; blocks creation when seat cap exhausted. CLI mint tool `prisma/mint-license.mjs` for ops: `node prisma/mint-license.mjs --plan PRO --to "Adwitiya Global" --months 12 --seats 10`. Sidebar Settings group gets License entry. Smoke test: PRO key minted → tampering correctly rejected → DB persist → read-back features intact → hasFeature(banking)=true, hasFeature(api)=false → 364 days remaining → 3/5 seats used. Build + typecheck clean. Phase 2 (gates on banking + GST routes, license-required redirect for expired installations, REST API for ops to mint/revoke remotely) queued.
- [x] **#134 / #106 Multi-Company A2 — Create Company button + topbar switcher + key isolation**: New `UserCompany` join table (userId + companyId + role, unique pair) grants access per non-ADMIN user; ADMIN sees all active companies. Cookie-based active-company state (`ftv-active-company`, httpOnly, 30-day) — `getActiveCompanyId()` now reads it, falls back to primary, validates the company is active. `getAccessibleCompanies(userId, role)` returns the dropdown menu. `setActiveCompanyId({companyId, userId, role})` writes the cookie after verifying access. **Series.docType uniqueness changed to (companyId, docType) composite** — each company gets its own PO-/GRN-/INV-/JV-/BT- counters starting fresh. `src/lib/series.ts::nextDocNumber()` now auto-creates a per-company series row on first use using a DEFAULTS map (PO/GRN/INV/JV/BT/WAREHOUSE/TR/OC/DN/CN/SA/ORP), removing the need for ensureXSeries() callsites — gutted those across transfers, other-charges, or-payments, stock-adjustments, warehouses. New `/companies` page lists every company with isolated counts (vendors / items / customers / POs / GRNs / sales / banks / CoA + active + primary badges) + Switch and Delete actions. New `/companies/new` with `CompanyForm` (legalName, brandName, state, GSTIN, address, city, pincode, email, mobile). `createCompany` server action: creates Company → grants OWNER UserCompany to creator → seeds default GSTIN if provided → seeds 50-account standard CoA with codes suffixed by the company's id slice to dodge global code uniqueness → seeds PO/GRN/INV/JV/BT series → switches the cookie to the new company. `switchActiveCompany` redirects to /dashboard with revalidatePath("/", "layout"). New `CompanySwitcher` client component in the Topbar shows the active company name + dropdown with all accessible companies + Manage / New buttons. Sidebar gets a "Companies" leaf next to Dashboard. CoA `accountIdByCode(code)` made company-aware: matches `code` exactly OR `code-*` prefix so the suffixed Company B codes resolve correctly. `nextChildCode()` for Customer/Vendor/Bank sub-ledgers now scoped to active company AND prefixed with the company id slice for non-primary companies to keep the global `code` uniqueness happy. Customer/Vendor/Bank create stamps companyId; ensureCustomerCoA / ensureVendorCoA / ensureBankCoA stamp companyId on the sub-ledger. **Key list pages scoped:** Vendors, Items (incl. Category lookup), Customers, Bank Accounts. **Key create actions stamp companyId:** Vendor, Item (incl. bulk import), Customer (incl. bulk import), Bank Account. Other modules (PO/GRN/Order/Sale/Payment/etc.) still global-read — they'll be migrated in Phase B. **Backfill script** grants admin (ankur) access to primary company. Smoke-tested: Adwitiya (5 vendors, 8 items, 56 CoA) + Company B "TestCo" via inline createCompany clone (0 vendors, 0 items, 5 CoA suffixed, 5 series) → isolation verified, admin has access to both, cross-contamination zero. Build + typecheck clean. Phase B continues with the remaining ~15 list pages + create actions + Company B test data fixtures + gap pass.
- [x] **#121-123 Wrap-up trio** (the half-built loose ends): (1) `Customer.gstRegType` field + dropdown + CSV round-trip + idempotent backfill. (2) `Vendor.gstRegType` field — same enum + treatment; backfilled 4 GSTIN-bearing vendors on dev DB to REGULAR; spells out ITC + RCM consequences in the form help text. (3) `MarketplaceOrder.customerId` FK + RecordSaleDialog gains a **ship-from Warehouse picker** (drives proper intra/inter-state CGST/SGST math — no more hardcoded "Uttar Pradesh" hack) and a **Customer picker** (drives B2B/B2C classification via the new gstRegType). Live preview pills now show "Classified as B2B/B2C" the moment admin picks a customer. B2B/B2C tile on the Orders Totals card lit up — aggregates from `customer.gstRegType` when present, falls back to B2C for unmatched (marketplace) orders. All three commits backed by tag `pre-wrapup`.
- [x] **Journal sidebar rename + promote**: "Other (Dr / Cr)" moved out of the Payment group up to a **top-level sidebar entry** named **"Journal"** with the NotebookPen icon and `alt+j` shortcut. Page title at `/other-charges` flipped to "Journal" with a forward-pointing note that Customer-side journal entries will land here once the Banking module (#124) ships. Route URL stays `/other-charges` — no bookmark breakage.
- [x] **#125 / #107 Phase 1 Accounting Engine** (Chart of Accounts + Journal + 3 reports): Three new tables — `ChartOfAccount` (Tally-style 4-digit codes, hierarchical via parentId, optional FK back to Customer/Vendor for auto-created sub-ledgers), `JournalEntry` (voucherNo via JV- series, date, narration, source MANUAL or AUTO_*), `JournalLine` (Dr / Cr / accountId — cascade-deletes with parent). Seeded **50 standard accounts** covering all the basics (Assets 1000-1199, Fixed Assets 1200-1299, Liabilities 2000-2299, Equity 3000-3299, Income 4000-4299, Expenses 5000-5399). Helper lib `src/lib/accounting.ts` exposes `createJournalEntry()` (validates balance, server-side enforced), `ensureCustomerCoA()` / `ensureVendorCoA()` (idempotent sub-ledger create — auto-generates sequential "1130-001" / "2110-001" codes per parent), `getTrialBalance({asOf})`, `getProfitAndLoss({from, to})`, `getBalanceSheet({asOf})` (with Retained Earnings rolling lifetime P&L so books balance pre-year-close). Customer.create / Vendor.create wired to auto-create paired CoA rows — failure is logged but does NOT roll back the master create. Backfill script ran on dev DB: 5 vendor sub-ledgers created (2110-001 through 005), 0 customers. Five new routes under `/accounting`: chart, journal (create form + list, delete restricted to MANUAL source), trial-balance, pnl, balance-sheet (Indian FY April default, full sign math, balance-check warning when off). New sidebar group "Accounting" with Calculator icon. Rollback tag: `pre-accounting-phase-1`.
- [x] **#126 / #107 Phase 2 Accounting Engine** (auto-post Sale / GRN / Payment): `src/lib/accounting.ts` extended with three idempotent posters — `postSaleJournal(orderId)` (Dr Sundry Debtors `1130-xxx`, Cr Sales Income `4110`/`4130` by channel + GST Payable `2120`; auto-flips for RETURN / RTO), `postGRNJournal(grnId)` (Dr Inventory `1140`, Cr Sundry Creditors `2110-xxx`; auto-flips for RTV), `postPaymentJournal(paymentId)` (Dr Sundry Creditors, Cr Cash `1110`; skips PENDING). All keyed by `(source, sourceRefId)` so re-posts are no-ops via `hasAutoEntry()`. New `reverseAutoJournal(source, sourceRefId)` deletes prior auto JVs for status-flip scenarios. Wired into: `orders/actions.ts` (importMarketplaceOrders batch, createOrder, bulkDeleteMarketplaceOrders pre-delete reverse), `grn/actions.ts` (createGRN non-draft, promoteDraftGRN, deleteGRN post-delete reverse), `payments/actions.ts` (upsertPaymentStatus reverse + re-post to handle PENDING↔PAID flips). One-shot `prisma/backfill-accounting-phase-2.ts` ran clean on dev DB — 19 Sales + 6 GRNs + 2 Payments posted, 0 errors; trial balance verified Dr = Cr = ₹445,186.41 (diff ₹0.00). Auto-post failures log to console.error but don't roll back the underlying master operation. Customer ledger code `1130-xxx` auto-created on first sale to a new customer. Rollback tag: `pre-accounting-phase-2`.

## ▶ RESUME HERE (next session, in C:\dev\FTV)
Correction batch is essentially COMPLETE. Done overnight 28→29-05:
- Model Master full CRUD (create/edit/delete) + vendor validation now accepts any active Model Master code
- Sales "Return" error fixed (Record-Sale form quantities now follow the selected Type)
- Item Master: bulk CSV import (per-row errors) · Category/Vendor/Model filters + filtered CSV export · per-row "Update price" modal (appends ItemPriceRevision)
- Other Charges module (/other-charges, OC- series) → debit in admin + portal ledger
- Stock Transfer upgrades (/transfers): TR- doc no, transfer type SJIT/SOR/Other+create-new, filters, CSV import/export/template
- Stock Adjustment module (/stock-adjustments, SA- series, signed qty, optional warehouse) → flows into Stock Report (Adj column)
- Reject Out / RTV module (/rtv) reusing the GRN engine (type locked to RTV)
- Stock Report: vendor + model filters + filtered CSV export

- Warehouse-wise Stock Report DONE (/warehouse-stock) — user chose per-document warehouse picker: GRN + Sale + Record-Sale + sales import now carry warehouseId; legacy rows show under "Unassigned".

REMAINING (deferred / optional):
1. Other Charges PDF / Email per charge — deferred (low priority; CSV export already there).
2. Playwright smoke test (task #33) — still open.
3. Resolve stray "Also Remove ??" note (line ~197) with user.
4. (optional) Backfill warehouse on existing GRNs/Sales so they leave the "Unassigned" column.
5. ⚠️ PROD PREP: dev schema changes were applied via `prisma db push` (no migration files since 20260526205507). Before prod deploy, regenerate migrations for: Warehouse, ModelMaster, Unit, OtherCharge, StockAdjustment, WarehouseTransfer.docNo/transferType, GRN.warehouseId, Sale.warehouseId, Warehouse relations.
Rollback points: `git reset --hard backup-pre-warehouse-fk` / `backup-correction-batch-done` / `backup-pre-othercharges`.

## ⬜ Multi-Model Vendor redesign — DISCUSSED, NOT STARTED (do not build until confirmed)

Goal: one real vendor (single email/GST/PAN/bank) can supply under **multiple models**
(FTV + OR), with **one dashboard** but a **separate ledger per model**.

DECISION (locked): **a SKU is ONE model at a time** (either FTV or OR), changed in Item
Master with an effective date. No concurrent both-model on one SKU (avoids the
fungible-stock allocation problem — warehouse can't tell identical units apart).

Design (locked):
- Vendor = single entity. `Vendor.model` demoted (no longer authoritative). One email/GST/PAN/bank.
- Model becomes **effective-dated on the item**, folded into ItemPriceRevision → `{price, tax, model, effectiveDate}`. Resolver returns price+tax+model by transaction date (purchase + sale).
- Snapshot resolved **model onto Sale and GRNItem** at transaction time (history-safe, like unitRate).
- Stock stays per-SKU (no per-model split needed — no concurrency).
- ModelMaster gains `paymentBasis` (ON_SALE | ON_GRN) + `paymentTermDays`: FTV/FTV_NORETURN = ON_SALE/0; OR = ON_GRN/45.
- Ledger model-aware: OR = GRN credit, due GRN+45, RTV/charges/payments debit; FTV = net-sold value credit, charges/payments debit (FTV GRN = stock-in only, NO credit).
- Vendor dashboard: FTV tile + OR tile + combined. Vendor code becomes a plain identifier (existing `ANOK-FTV` codes stay, cosmetic).

Assumed defaults (unless overridden): new vendor codes drop `-MODEL`; backfill existing items/sales/GRNs with vendor's current model.

OPEN CONFIRMATIONS before build:
1. Model switch while stock on hand → recommend **block switch until on-hand = 0** (vs FIFO-by-receipt attribution).
2. FTV settlement trigger = sale event, accrued monthly, net of returns/RTO? (vs marketplace remittance).
3. Returns asymmetry: OR customer-return = stock only (no vendor effect), only RTV reduces OR payable; FTV return claws back payout. Confirm.
4. FTV goods received via GRN = stock-in only, no ledger credit. Confirm.

Phasing: Phase A (foundation, no money change) then Phase B (money). Backup tag `backup-pre-multimodel`.
Group-1 decisions LOCKED: A1 new codes plain (existing untouched); A2 backfill with vendor's current model; A3 approval just activates vendor; A4 block model switch until on-hand=0.

PHASE A1 DONE (commit): ItemPriceRevision.model, Sale.model, GRNItem.model, ModelMaster.paymentBasis(ON_SALE|ON_GRN)+paymentTermDays; resolvePrice() returns model; backfill stamped existing revisions/sales/GRN lines with vendor model + set OR=ON_GRN/45, FTV&FTV_NORETURN=ON_SALE/0. (prisma/backfill-model.ts)
PHASE A2 DONE (commit): Item create/edit + "Update terms" modal require model (effective-dated); bulk import reads model column (default vendor model); switch guard blocks model change while on-hand!=0 (getOnHandQty in src/lib/stock.ts); GRN lines + Sales (manual+import) snapshot model by transaction date; item list shows latest-revision model. No money-logic change yet.

PHASE A REMAINING (optional cleanup):
- A-step 3 DONE (commit 3a89c5d): removed Model from Vendor Master (form, approval/review flow, vendor table + CSV import/template), plain vendor codes (computeVendorCodeBase/nextFreeVendorCode, no -MODEL suffix; existing codes untouched), approval just activates the vendor. Vendor.model DB column KEPT DORMANT (no drop). All display read sites now resolve model from the item's latest price revision (priceRevisions[0].model) with vendor.model fallback for legacy rows: admin+portal PO detail, PO PDF buildPdf, admin+portal sales, sales CSV export, portal payments, daily-report cron, stock + warehouse-stock (model filter applied post-computation, not at DB level), portal items. Write/derivation paths (sales/grn/items actions) keep vendor.model as fallback only when no revision model exists. tsc + lint + next build all green.

PHASE B (money) — Group-2 LOCKED: B1 FTV=net-sold(sold-ret-RTO)*rate+GST monthly; B2 OR due=GRN+45 per receipt; B3 returns asymmetry; B4 FTV GRN=stock-in no credit; B5 payments per-model; B6 dashboard tiles.
PHASE B1 DONE (commit): src/lib/vendor-ledger.ts buildVendorLedger() + admin /ledger model tabs (Combined/per-model) + Due column + tiles (FTV payable, OR payable+overdue, combined, this-month FTV sales). Portal ledger reuses engine (combined). FTV GRN no longer credits; OR credited at GRN with due.
PHASE B2 DONE (commit): OrPayment voucher table + /or-payments running-ledger (OR GRN receipts auto-listed: Invoice Date/GRN Date/Particulars/Amount/Due=GRN+term/overdue; free-form payment entries; running balance + Billed/Paid/Balance/Overdue tiles + CSV). FTV Payment page (/payments) filtered to ON_SALE models, uses sale snapshot model, nets sold-returns-RTO. Ledger engine: FTV payments from Payment (ON_SALE), OR payments from OrPayment vouchers. Migrated legacy OR Payment-table rows → OrPayment. Sidebar: FTV Payment + OR Payment links. ✅ FTV/OR MULTI-MODEL COMPLETE (Phase A + B).

### ✅ Inventory Valuation (FIFO) — DONE (commit 15843c2)
New /inventory-valuation under Inventory. src/lib/inventory-valuation.ts: on-hand qty mirrors Stock Report; cost layers = PURCHASE+RFV GRN lines (qty×rate); on-hand valued oldest-receipt-first (FIFO by grnDate), so remaining stock carries latest purchase rates; on-hand beyond layers (opening/positive adj) valued at latest transfer price (flagged *). One pooled price per SKU; dynamic WH qty columns; tiles + CSV export.

### ✅ Margin Report + Marketplace imports — DONE (commits 124c794 foundation, afd5902 feature)
Decisions LOCKED (from user's Margin Report.xlsx + Marketlace Order Tab.xlsx): super-admin = User.isSuperAdmin flag (DB-checked, no re-login); marketplace data = separate line-level MarketplaceOrder table with a Type col (SALE/RETURN/RTO) added to the Common Order format; marketing = MarketingCost per SKU+month; commission+logistics = % per marketplace (MarketplaceRate) applied to NET sale (assumption — confirm if gross); Sale Value = Taxable (pre-GST); show BOTH sheet Margin and Net Margin after Transfer Price (COGS).
- Foundation: schema (isSuperAdmin, MarketplaceOrder, MarketingCost, MarketplaceRate via db push); requireSuperAdmin()/isSuperAdmin() in rbac.ts; Users screen Super toggle (ADMINs only).
- New "Marketplace" sidebar group: Marketplace Orders (import common order format), Marketing Cost (import Month·SKU·Spent), Marketplace Rates (commission%/logistics%), Margin Report (super-admin).
- Margin engine src/lib/margin-report.ts: per-SKU; Net Sale = Sale−Return−RTO (taxable); Commission/Logistics = Σ signed-line taxable × marketplace %; Marketing from tab; Margin = NetSale−Comm−Logi−Mktg−Other(0); COGS = transferPrice×netQty; Net Margin = Margin−COGS; filters month/vendor/model/marketplace; CSV export.
- FOLLOW-UPS: #78 native Amazon/Myntra→common-format mappers (DEFERRED, big effort); #79 manual "Other" per SKU/month (v1 shows 0); confirm commission/logistics on net vs gross sale.

ARCHITECTURE (agreed for scalability — more models coming): organize payment/settlement screens by paymentBasis (calculation type), NOT by model name. Model Master is the registry — each model declares its basis + term. Adding a new model of an EXISTING basis = zero code (auto-appears in the right screen + ledger). A genuinely NEW calculation = a new basis + its own screen/branch. TODO: make paymentBasis + termDays editable in the Model Master create/edit form (currently set only by seed/backfill) so new models are self-service. Ledger is already basis-driven (tabs per model present).

### ⬜ Dropship — FUTURE 3rd payment basis (after FTV/OR Phase B) — NOT STARTED
Sell vendor items on OUR website (multi-vendor). VENDOR SHIPS directly. We charge commission on the customer sale price. Vendor payout = salePrice − commission − logistics.
- New basis `DROPSHIP` = pay-on-sale, payout = net-of-deductions (NOT transfer price). e.g. ₹1000 sale − 15% (₹150 our revenue) − ₹60 logistics = ₹790 to vendor.
- KEY: dropship items have NO inventory on our side — vendor ships → NO GRN / stock-in / stock balance / transfer. Pure passthrough: Order → Ledger → Payment.
- Needs: Model Master "Dropship" (basis=DROPSHIP + default commission %); Sale fields salePrice/commission/logistics (from website order feed); dropship order import; buildVendorLedger DROPSHIP branch (monthly credit = Σ(salePrice−commission−logistics)); "Dropship Payment" settlement screen; NEW Commission/Earnings report (commission = OUR revenue, unique to dropship).
- Open decisions: commission % scope (per model/vendor/category + override); logistics actual-vs-flat; order source (CSV/API); GST on commission (GST phase).

## ⬜ Menu / Sidebar restructure — DISCUSSED, NOT STARTED (user will finalize structure first)

Goal: regroup the flat admin sidebar into collapsible main-menu groups with sub-items.
User will refine the structure before we build. Vendor portal nav: TBD (probably leave as-is).

Proposed groups (existing pages unless marked NEW):
- Dashboard
- Masters ▾ : Vendor · Item · Category · Warehouse · Model
- Purchase ▾ : Purchase Orders · GRN/Purchase · Reject Out/RTV · Debit Note (NEW) · Credit Note (NEW)
- Sale & Return  (= current Sales page, renamed)
- Payment ▾ : Payment · Other (Other Charges)
- Ledger ▾ : Vendor Ledger
- Inventory ▾ : Overall Inventory (Stock Report) · Warehouse Stock · Warehouse Transfer · Stock Adjustment
- Support Ticket ▾ (NEW) : Call Back Now · Email · WhatsApp
- Training (NEW)
- Policies (NEW)
- Settings ▾ (admin) : Document Series · Master Code Prefix (NEW) · Email Configuration (NEW) · Document Format (NEW) · [Users & Invites?] · [Audit Log?]

Regroup-only (no new code): Masters, Inventory, Payment/Ledger, Sale&Return, PO/GRN/RTV.
NEW builds: Support Ticket (call/email/WhatsApp), Training, Policies, Master Code Prefix, Email Configuration, Document Format.
  - Debit Note: REMOVED from menu per user (29-05).
  - Credit Note → renamed "Reject-In / RFV" and BUILT (29-05): GRN type RFV, mirror of RTV — goods in from vendor, inventory +, OR credited (due GRN+term), FTV stock-only. /rfv + /rfv/new, Stock Report RFV column, ledger + OR Payment credit.

OPEN QUESTIONS for user:
1. Vendor Ledger — under Payment or its own Ledger group? (listed both) → recommend its own Ledger group.
2. Purchase Orders — under Purchase group? (not named) → assume yes.
3. Users & Invites + Audit Log placement → recommend under Settings (admin).
4. Dashboard pinned at top.
5. Debit Note = doc for RTV/charges to vendor? Credit Note = vendor's credit to us (or customer sales returns)? — decides ledger posting.
6. Support Ticket = quick contact actions (callback form / mailto / wa.me) or a full ticket system?
7. Training / Policies = static content (user-supplied text) or admin-editable pages?
8. Settings → "Document Format" = the previously-DROPPED editable PDF template, or just light toggles (logo/address/terms)?
9. Email Configuration: API key stays env (security); DB stores from-name/email/reply-to + per-doc toggles/templates only.

Build sequence: Phase 1 = sidebar regroup with existing pages (new items shown disabled/"coming soon"); Phase 2+ = build new modules one at a time.

PHASE 1 DONE: admin Sidebar regrouped into collapsible groups (Masters · Purchase · Sale & Return · Payment[Payment/Other/Vendor Ledger] · Inventory · Support Ticket · Training · Policies · Settings[Series + Users + Audit + 3 soon]). Decisions applied: Vendor Ledger under Payment; PO under Purchase; Users+Audit under Settings; unbuilt items show greyed "soon". Collapsed mode shows group icons; clicking a group expands the rail + opens it.
PHASE 2+ REMAINING (new builds, awaiting scope): Debit Note · Credit Note · Support Ticket (call/email/WhatsApp) · Training · Policies · Master Code Prefix · Email Configuration · Document Format. Also: regroup vendor portal sidebar (TBD).

## ⬜ Typography change — NOT STARTED
Switch fonts: ALL headings & subheadings → **Poppins**; ALL body text → **Lato**.
Current: Cormorant Garamond (display/headings, used via `font-display`) + Plus Jakarta Sans (body/sans).
Implementation: swap the `next/font/google` imports in `src/app/layout.tsx` (Poppins + Lato, set CSS vars); update `tailwind.config.ts` fontFamily (`display` → Poppins, `sans` → Lato); verify `font-display` (headings h1/h2/section labels) + default body all resolve correctly; check weights used (Poppins 600/700, Lato 400/700). Update the stack note in CLAUDE.md §3.

## 🤖 Autonomous Session — 2026-05-30 (user asleep, "take best call + record why")

User mandate: (1) build a Stock Ledger (movement tracking + filters); (2) walk every module, check consistency, fix what's safe, plan the rest for discussion; (3) record rationale for every call; (4) create demo data + walk each process. Decision log below.

DECISION LOG:
- D1 — BUILD the Stock Ledger now (not just queue it). Rationale: user explicitly wants demo data "processed at each step", which needs the ledger to exist; it's read-only, low-risk, aggregates existing data, and doubles as a consistency check (its running balance must equal the Stock Report). #80.
- D2 — Stock Ledger = TOTAL on-hand running balance per SKU (bank-statement style). Transfers shown as info rows but DO NOT change the total balance (they're internal location moves; the Stock Report total also ignores them). Keeps the ledger reconciled with the Stock Report. Single-SKU view = full running balance; multi-SKU = per-SKU balance on each row.
- D3 — Running balance computed over the SKU's FULL history; date/type/warehouse filters only hide rows for display (balance stays a true point-in-time figure). Vendor/model/SKU filters reduce the item set.
- D4 — FIX getOnHandQty (src/lib/stock.ts): it treated RFV as stock-OUT (grouped with RTV as `-accepted`); RFV is a Reject-IN (+). Confirmed against Stock Report / Warehouse Stock / Inventory Valuation which all treat RFV as +. Low-risk, clearly correct; affects the model-switch guard. Rationale recorded; fixed inline.
- D5 — REWRITE prisma/demo.ts to match the current system (was pre-multi-model): plain vendor codes (no -MODEL), set revision.model + Sale.model + GRNItem.model snapshots, add an RFV GRN, OrPayment vouchers, MarketplaceOrder lines, MarketingCost, MarketplaceRate. Needed so the demo actually exercises FTV/OR money logic + the new Marketplace/Margin modules.

AUDIT FINDINGS + IMPROVEMENT PLAN (walked every module; verified with demo data via prisma/verify.ts — an independent recompute that reconciled Stock / Valuation / Ledger / Margin):
- ADMIN VENDOR APPROVAL ✅ DONE — Admin-created vendors now land in PENDING (force-set in createVendor + bulk-import create path; appliedAt stamped so they sort with /apply applications). After Create, admin is redirected straight to /vendors/[id]/review to approve and send the portal invite. Edit-time guard: updateVendor blocks silent PENDING→ACTIVE via the regular edit form (must use the review screen), but still allows ACTIVE↔INACTIVE deactivation. Bulk-import UPDATE path preserves the existing vendor's status so a CSV that omits status won't accidentally demote ACTIVE vendors back to PENDING. New-vendor form replaces the status dropdown with an amber banner explaining the flow.
- F1 ✅ FIXED — getOnHandQty (model-switch guard) treated RFV as stock-OUT; now RFV adds (only RTV subtracts). Matches Stock Report / Warehouse Stock / Inventory Valuation / Stock Ledger.
- F2 ✅ FIXED — WarehouseTransfer now updates the Warehouse Stock per-warehouse pivot. Added fromWarehouseId + toWarehouseId FKs (legacy type/location kept nullable for old rows); transfer form picks From + To warehouses; warehouse-stock pivot subtracts qty from source + adds to destination; Stock Ledger shows "FROM → TO" using warehouse codes. Verify harness extended (section 5) confirms per-warehouse Σ = total stock for every SKU. Also fixed an analogous RFV-as-stock-out bug in the warehouse-stock pivot (PURCHASE+RFV now both add; only RTV subtracts), matching the earlier getOnHandQty fix. Original note below for context.
- F2 (history) ⚠️ — WarehouseTransfer did NOT update the Warehouse Stock per-warehouse pivot. warehouse-stock computes balances from GRN+Sale+Adjustment only; transfers are ignored, so a SENT_TO_WH move doesn't reduce the source WH or raise the dest WH in the pivot. TOTAL on-hand is still correct (transfers net to zero). ROOT CAUSE: WarehouseTransfer uses a free-text `location` + type SENT_TO_WH/RETURN_TO_HO, not from/to warehouseId FKs, so it can't be mapped to the warehouse columns. PLAN (needs your call — schema change): add fromWarehouseId + toWarehouseId to WarehouseTransfer (keep `location` for legacy), update the transfer form + warehouse-stock pivot + Stock Ledger to move qty between warehouses. NOT done autonomously (design decision + schema migration).
- F3 ✅ FIXED — stale GRN.type schema comment ("PURCHASE | RTV") now documents RFV too.
- F4 ⚠️ DISCUSS — no negative-stock guard: sales/adjustments can drive on-hand below 0 (marketplaces can oversell). Currently allowed silently. PLAN: optional soft-warning on Stock Report / at sale import for SKUs that go negative. Low priority; confirm if wanted.
- F5 ℹ️ OK — Stock math is consistent across Stock Report, Warehouse Stock (total), Inventory Valuation, Stock Ledger, and getOnHandQty (all: purchase+rfv−rtv−sold+ret+rto+adj).
- F6 ℹ️ ASSUMPTION (already flagged) — Margin commission/logistics applied to NET sale per marketplace; confirm vs gross.
- F9 ✅ FIXED — prisma/demo.ts was pre-multi-model (set no revision/Sale/GRN model snapshots, -MODEL codes, no RFV/OrPayment/marketplace/marketing data). Rewritten to current system + every sold SKU now has inward stock (no negatives).
- F10 ✅ ADDED — prisma/verify.ts: reusable read-only consistency harness (recomputes Stock/Valuation/Ledger/Margin from raw rows). Run: `npx tsx prisma/verify.ts`.

WALKTHROUGH RESULT (demo): all SKUs non-negative; Inventory value ₹196,470; Rajwadi OR balance 56,814.40, Anokhi FTV 20,303, Delhi FTV 9,826.20 (hand-verified); Margin May netSale 38,269 → margin 25,104.93 → net margin 1,524.93. Everything reconciles.

## 🤖 Autonomous Session — 2026-05-30 (part 2: go-live hardening)

User mandate: make fields mandatory where sensible; warn before OVERWRITING old data; verify every import/export template; we go live in 2-3 days (heavy testing ahead).

DECISIONS:
- D6 — OVERWRITE WARNING on the 3 bulk imports that modify existing rows: Item (by skuCode), Vendor (by email), Marketing Cost (by SKU+month). Two-phase: the action counts how many existing rows would be overwritten and returns needsConfirm WITHOUT writing; the UI shows a ⚠️ confirm with the count; only on OK does it re-run and commit. Append-only imports (Sales, Marketplace Orders) don't overwrite, so no confirm (re-import = duplicates, not overwrite — noted for the test checklist).
- D7 — MANDATORY warehouse on GRN + manual Sale (grnSchema/recordSaleSchema + forms now require it, with a * and a client guard). Rationale: accurate per-warehouse stock; both are form-only paths. IMPORTANT: the Sales CSV IMPORT stays lenient (warehouse optional → Unassigned) so existing sheets still load — only manual single-entry enforces it. Reversible if you disagree.
- D8 — Did NOT make vendor email/GST/PAN mandatory. Rationale: formats are already validated when present; forcing them would block legit quick-adds and the apply→approve flow. Left to your discretion (easy to flip later).
- D9 — Added a Sales import Template button (only module that lacked one) and fixed the Item template (was the old ANKU-FTV code; now plain "ANOK" + a model column).
- D10 — Added prisma/check-templates.ts: round-trips every import/export template through papaparse and asserts the importer's required columns survive. All 5 pass. Run: `npx tsx prisma/check-templates.ts`.

### ✅ GO-LIVE MANUAL TEST CHECKLIST (do during the 2-3 day testing window)
Reset to a clean demo first: `npx tsx prisma/demo.ts` then `npx tsx prisma/verify.ts` (should report no negative stock + reconciled ledgers). Grant yourself super-admin in Users for the Margin Report.
- [ ] Vendor: create (name only), edit; import CSV (Template) — re-import same file → expect OVERWRITE ⚠️; bad GST/PAN/IFSC row → row error.
- [ ] Item: create (model + price required); import (Template) — re-import → OVERWRITE ⚠️; model-switch while stock>0 → blocked.
- [ ] PO → GRN (warehouse now REQUIRED) → partial receive → status PARTIALLY_RECEIVED/CLOSED.
- [ ] RTV (stock −) and RFV (stock +); confirm Stock Ledger rows + Stock Report balance.
- [ ] Sale manual (warehouse REQUIRED); Sales import (Template, warehouse optional) → re-import duplicates (no overwrite — expected).
- [ ] Stock Adjustment ±; Warehouse Transfer (note: not yet in per-warehouse pivot — see F2).
- [ ] Other (Dr/Cr) note → vendor ledger moves the right way.
- [ ] FTV Payment + OR Payment → balances reconcile with Vendor Ledger.
- [ ] Marketplace Orders import (Template, SALE/RETURN/RTO) ; Marketing Cost import → re-import → OVERWRITE ⚠️; Marketplace Rates set %.
- [ ] Margin Report (super-admin) for the month → tiles + net margin sensible.
- [ ] Every list page: Export CSV opens cleanly in Excel (formula-injection guard prefixes =/+/-/@).
- [ ] Stock Ledger / Inventory Valuation / Batch Report render and reconcile.

## Open questions / parked items

- WhatsApp Cloud API account — wait until Phase 6 (deep-link `wa.me` works until then)
- Customizable workflow / per-vendor approval rules — out of scope unless asked
- Mobile app — out of scope
- ⚠️ Working dir: canonical project is now `C:\dev\FTV` (moved off OneDrive after a sync wiped files). Always run Claude Code + dev server from there.
